# ไฟล์ที่ **เกินเพดาน 500 บรรทัดอยู่แล้ว** และแตกในใบที่พบไม่ได้ — หนี้ของ scripts/check-file-length.sh (task 193)
#
# กติกา (เหมือน scripts/card-paths-allowlist.txt เป๊ะ):
#   * หนึ่งพาธต่อบรรทัด ตามด้วย `# task NNN — เหตุผล` เสมอ — แถวที่ไม่บอกที่มา = แถวที่ไม่มีใครรื้อได้อีก
#   * ไฟล์ลดลงต่ำกว่าเพดานแล้ว = FAIL (allowlist ห้ามเป็นสุสาน)
#   * ไฟล์หายไปแล้ว = FAIL เช่นกัน แต่ **คนละข้อความ** เพราะเป็นคนละทางแก้
#   * ไฟล์อยู่นอกขอบเขตกติกา §4 อยู่แล้ว = FAIL ทิศที่สาม — แถวแบบนั้นปลดไม่ได้เชิงตรรกะ
#     ⇒ ถ้าตั้งใจให้อยู่นอกขอบเขตจริง ที่ของมันคือ scripts/lib/file-length-scope.sh ไม่ใช่ที่นี่
#
# ⚠️ **นี่ไม่ใช่ที่ซุกไฟล์ที่ขี้เกียจแตก** — ใบ 193 เลือก **แตกจริง** ทั้งสามไฟล์ที่เกินอยู่
# (`check-sql-coverage.sh` · `generate_billing_pdf.mjs` · `check-counter-test-selftest.sh`)
# เพราะสามใบนั้นคือ *ตัวเกตเอง* และกติกาที่เกตของมันเองละเมิดอยู่ สอนคนอ่านว่า §4 เป็นแค่ความปรารถนา
# ⇒ แถวที่เหลือในไฟล์นี้จึงมีได้เฉพาะของที่การแตกเป็น **การตัดสินใจของผลิตภัณฑ์** ไม่ใช่การจัดบ้าน

