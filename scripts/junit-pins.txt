# หมุดชั้นที่หนึ่งของ `scripts/lib/check-code-junit.sh` — ไฟล์เทสที่ต้อง **ถูกรันจริง**
# และ **รันครบตามจำนวนที่ปักไว้** · รูปแถว:  <path เทียบรากรีโป><TAB><จำนวนเทสที่รันจริง><TAB># ใบ NNN · เหตุผล
#
# 🔑 **ทำไมเลขอยู่ในไฟล์ข้อมูล ไม่ใช่ในสคริปต์** — มันคือสิ่งเดียวที่ทำให้เงื่อนไข
# "เท่ากันเป๊ะ" จ่ายไหว: เลขนี้ขยับทุกครั้งที่ใครเพิ่มเทสหรือเพิ่มแถวในตารางที่ลูปอ่าน
# ⇒ ถ้าเลขอยู่ในเชลล์ คนที่ทำสิ่งที่ดีและเกิดบ่อยจะถูกเก็บภาษีเป็นการแก้สคริปต์
# ⇒ และจะจบด้วยการมีคนลดหมุดทิ้งเพื่อให้เขียว · แก้ไฟล์ข้อมูลหนึ่งบรรทัดคือราคาที่ถูกต้อง
#
# ⚠️ **ลดเลขลงได้ แต่ต้องเขียนเหตุผลในใบงาน** — การลดเลขคือการประกาศว่า coverage หายไป
# ห้ามลดเพื่อให้เกตเขียว · และ **การแตกไฟล์เทสจะทำให้เลขตก ⇒ แดง** ซึ่งตั้งใจให้เป็นแบบนั้น
#
# ชื่อคือ **path เทียบรากรีโป** เพราะชื่อ testsuite ใน junit คือ path เทียบ cwd ของ `bun test`
# (รากรีโปทั้งโหมด native และ `/app` ในคอนเทนเนอร์) — ต่างจาก groove-clinic ที่เทียบ web/
# เพราะที่นั่นเว็บเป็นแพ็กเกจย่อย ส่วนที่นี่เทสอยู่ข้าง ๆ โค้ดที่รากเลย
#
# 🔴 **จำนวนคือ `tests - skipped`** — `lib/parser.test.ts` มี 16 เทส skip 3 (ฉากที่ต้องมีไฟล์
# ชีตจริงถึงจะรันได้) ⇒ หมุด 13 · วันที่ไฟล์จริงเข้ามาแล้ว skip หายไป เลขจะขึ้นเป็น 16 และ
# เกตจะแดง **โดยตั้งใจ**: ให้คนที่เพิ่ม fixture เป็นคนยกหมุด ไม่ใช่ให้เกตเงียบ
lib/password.test.ts	7	# ใบ 001 · แฮช/ตรวจรหัสผ่าน — เส้นทางเดียวของ auth ทั้งระบบ
lib/parser.test.ts	13	# ใบ 001 · ตัวอ่านชีต (16 เทส skip 3 ที่ต้องมีไฟล์จริง)
lib/onboard.test.ts	4	# ใบ 001 · พนักงานใหม่เข้าระบบ — จุดที่คาบสอนกำพร้าเกิดได้
lib/payroll/teach.test.ts	6	# ใบ 001 · ค่าสอน 1-on-1 (§1.2) — one of six files the **ใบ 037 split** of `lib/payroll.test.ts` produced. That file's pin was 34 and it is gone; these six rows sum to **36 = the 34 that moved + the 2 ใบ 037 adds**, which is the honest form of "coverage moved, none removed" (§7 — a split lowers a pin and goes red on purpose). (+1 ใบ 034: an activity named `__proto__` configured at a real rate must not be inherited by any other activity — an unconfigured one still warns and stays unpaid instead of silently paying the polluted number · +2 ใบ 037: the `rate == null` boundary, which two one-character tidy-ups used to break while every assertion stayed green — a rate configured at exactly `0` pays 0 with `warnings: []` and **still emits its teach line** (the owner chose that number, so it is shown), a **missing** rate warns `ไม่มีเรทค่าสอน <activity> × <rank>` and emits **no** line. Both arms assert on `lines.filter(group === "teach")`, not on `teachPay`: the total is 0 either way and only the line tells the two apart (the ใบ 025 lesson). Counter-tested 2026-09-19, and the three mutants are worth different amounts — read this before lowering 6: `if (!rate)` fails the **zero arm and nothing else** (nothing in the old 34 caught it — that is what this card newly buys) · `continue` → `break` fails the **missing arm and nothing else** (it was green against all 36 until the missing arm was given a rated activity to sit beside; `[...byActivity].sort()` is lexicographic, so an unrated `aqua` is visited before `pt` and `break` would drop 12 คาบ = 4,800 ฿ behind one warning naming only the 4 aqua คาบ) · `?? 0` fails **three** — the pre-existing `ไม่มีเรท (Yoga)` and `__proto__` arms catch it too, because they assert the *warning*, not only `teachPay === 0`. ⇒ what the missing arm adds over yoga is the **line**, which no older arm asserted; it is not the sole guard, and the yoga arm is not redundant. ⚠️ The deliberate-`0` premise is still open at `tasks/todo-human/038-what-adding-an-activity-is-for.md` §1: if the answer is "never", the zero arm inverts and is rewritten, not deleted, so this pin stays 6.)
lib/payroll/class.test.ts	7	# ใบ 001 · ค่าสอนคลาส Group (§1.4) — from the ใบ 037 split of `lib/payroll.test.ts` (+3 ใบ 025: a negative attendance (`noShow > booked`) must warn with the two counts **in order** and contribute a class line of exactly 0 (`classPay` alone is clamped by `Math.max(0, …)` and cannot fail); an attendance of exactly 0 must NOT warn — the boundary is the sign; and one warning **per row** — `byClass` merges the lines by class name, the warnings must not follow it) (+1 ใบ 065: the ใบ 025 repair text branches on `ClassSessionInput.sourceKey`, and the arm asserts **both** directions — a message that always says `ลบคาบนี้แล้วคีย์ใหม่` is green against the hand-keyed half alone, and telling a reader to delete an imported row is what pays that คาบ twice — 200 + 200 on a Core Strength, §1.4's price table)
lib/payroll/commission.test.ts	14	# ใบ 001 · ค่าคอม PT (§1.5) · Incentive (§1.6) · ค่าคอมสมาชิก (§2.2) — from the ใบ 037 split of `lib/payroll.test.ts`. The three describes stay in one file because the incentive threshold is a property of the commission total rather than a separate subject, and §2.2 membership is the same `pct()` table read for a different `kind` — **not** because they share a fixture: `ptSale` moved to `lib/payroll/fixtures.ts` and `ค่าคอมสมาชิก (§2.2)` builds its own `sale()` factory anyway (+2 เตือนคอม ใบ 009: an unrecognised `sale.kind`, and a membership attributed to a non-closer, each warn **and** pay 0 — §7 item 8 has no rule for either, and inventing a config default to silence them is the thing this pin forbids)
lib/payroll/ot.test.ts	3	# ใบ 001 · OT (§2.4) — from the ใบ 037 split of `lib/payroll.test.ts` (+1 OT ทศนิยมยาว/ปัดครั้งเดียว ใบ 009: 9:20 arrives from a fingerprint export as 9.333333333333334, and the engine's reference answer — 13.33 · `qty` 0.33 · 266.67 over 20 days — is pinned here. `.docs/knowledge/domain/money-on-screen.md` quotes those three figures and lists this file as a source (it held them as `payroll-rules.md` rule 4 until the ใบ 042 split), so deleting the test lands there as STALE rather than passing under a card still quoting it)
lib/payroll/slip.test.ts	5	# ใบ 001 · สูตรรวม (§1.7) — every block sums to `net` and `lines` reconciles back to it — from the ใบ 037 split of `lib/payroll.test.ts` (+2 inactive staff warn-but-never-rezero, ใบ 013: a deactivated staff member with a period still owed warns about base that is **not** pro-rated, and the whole-result comparison against the active run is what catches someone later deciding to pay a leaver 0 "because they are inactive" — the silent zero §2 rule 4 forbids) · **3→5 ใบ 063**: ฐานเงินเดือน 0 ของเทรนเนอร์ต้องเตือน — `if (base) lines.push(...)` ทำให้ฐาน 0 ไม่มีบรรทัดเลย และ *บรรทัดที่ไม่มี* คือสัญญาณที่อ่อนที่สุดของสลิป ⇒ ของจริงที่วัดได้: ประพัฒน์ สอน 5 คาบ 1–21 ก.ย. คนเข้า 0 ทุกคาบ ⇒ `net 0.00 · warnings: []` สลิปศูนย์บาทของคนที่สอนไปห้าคาบ โดยไม่มีอะไรบอกว่ามีตัวเลขที่ยังไม่ได้ตั้ง · อาร์มที่สองกั้น **predicate** ไว้ (`role === "trainer"` ไม่ใช่ `!base`): owner ถูก seed เป็น 0 ตั้งใจ และ counter ยังไม่มีใครบอกว่าต้องมีฐาน ⇒ ถอด role check ออกแล้วอาร์มแรกยังเขียว · counter-test 2026-09-21: ปิดเงื่อนไขทิ้ง ฆ่าอาร์มแรก
lib/payroll/config-guards.test.ts	4	# ใบ 013 item 4 · num() ต้อง throw เมื่อค่า config ว่าง/ไม่ finite — ช่องว่างเคยอ่านเป็น 0 เงียบ ๆ แล้วจ่ายขาดทั้งระบบ · from the ใบ 037 split of `lib/payroll.test.ts`. 🔑 It stays in the **engine** suite, not next to `lib/config-keys.ts`, because its fourth arm is a `computePayslip` money assertion — a 20,000 ฿ self-closed bill pays 2,000 ฿, and clearing `comm.pt.selfClosed` throws instead of paying 0 — so it needs `lib/payroll/fixtures.ts`. Moving the file "to where its subject lives" drops that arm and takes the pin 4 → 3 without anyone calling it a lowering
lib/ot-import.test.ts	7	# ใบ 009 · แยกชื่อที่จับคู่ไม่ได้/ชั่วโมงที่ไม่ใช่ตัวเลข ออกจากไฟล์สแกนนิ้ว — ชั่วโมงที่หายเงียบ กับ NaN ที่ทำให้ทั้งสลิปเป็น NaN (+1 ใบ 013 · ชั่วโมงติดลบ: finite แต่จ่ายเป็นศูนย์ ⇒ เข้า invalidHours เหมือนกัน)
lib/next-errors.test.ts	6	# ใบ 009 · แยก redirect() ของ Next ออกจาก error จริง — ยามของ catch ใน server action ที่ /ot
lib/payroll-run.test.ts	6	# ใบ 013 · the run loop's pure decisions — the non-draft guard's predicate and wording, the two refusals staying distinguishable, and the period's staff scope (active · already holds a slip · did payable work in it). 5→4 in the same round it was added: the two scope tests merged into one whole-object comparison that pins strictly more (arm count, order, OR-not-AND, every period binding) — coverage merged, not removed (§7 'the expensive unit is the fixture'). No DB in bun test ⇒ the lock itself is reviewed, not pinned; see the file header. 4→6 in task 034: `buildTeachRates` was extracted from `runPayroll` (it lives in `lib/payroll.ts`, beside the type it builds, so the engine's own tests need no `lib/db.ts`; exercised here because `runPayroll` is its only caller) so the rate map could be asserted without a database — one test pins that an activity named `__proto__` never reaches `Object.prototype` (the half that outlives the request), one that several ranks of one activity merge instead of overwriting.
lib/form-number.test.ts	13	# ใบ 013 · the one predicate that guards every form field which becomes money — absent/blank/File/1e999/negative each refused, `isBlank` keeping a File out of the "not given" bucket, and (+2 รอบรีวิว) `int`/`max`: Postgres integer **ตัดทศนิยมทิ้งเงียบ ๆ** ไม่ได้ปฏิเสธ และค่าเกินช่วงจะ throw กลางลูปเขียน
lib/config-form.test.ts	12	# ใบ 013 · the /admin/config bulk save parsed all-or-nothing — a blank rate deletes, a blank salary refuses, and one bad field refuses the whole save (the screen promises exactly that) · (+5 รอบรีวิว) `cfg|` เข้ามาอยู่ในด่านนี้ด้วย (ช่องว่างเคยจ่าย 0 ฿), เพดาน Int, ทศนิยมที่เคยถูกตัดทิ้ง, และ staff field ที่ฟอร์มไม่มี
lib/staff-form.test.ts	9	# ใบ 027 · `addStaff` used to refuse an empty name / username / short password with a bare `return` — the page revalidated, the form cleared, nothing said the person had not been added. Nine arms over the four refusals this parse can return — `newstaffDup` has none, it comes from the unique index and would need a real Postgres — the **order** (name → username → password → money) so two bad fields always report the same one, the password boundary pinned to `MIN_PASSWORD_LEN` and **not** trimmed, and task 013 item 4's money guard proven unmoved (blank ⇒ 0, unreadable ⇒ refuse).
lib/activities.test.ts	5	# ใบ 036 · (targets `lib/activity-names.ts`, the db-free half — the review found the test dragging `lib/db.ts` into `bun test`, the coupling task 034 moved `buildTeachRates` out of `payroll-run.ts` to avoid) the activity-name union that makes an unpriced activity representable — `addActivity` used to seed all three ranks at `rate: 0`, so `rate == null` in the engine was unreachable for a screen-added activity and a 12-session ST trainer was paid 0 ฿ with `warnings: []`. Five arms on the pure fold: the four-way union deduped and sorted (whole-array compare, so the row order of the matrix is pinned too), an **empty registry taking nothing away** (the fail-safe direction — a rate row, a sheet source or one historical session still renders, so no rate can vanish because a name was not registered), trimmed-exact comparison (padding merges · case does **not**, so the unpriced twin warns instead of inheriting a rate), a blank name dropped rather than rendered as a nameless `rate||PT` row, and a prototype-key name (`__proto__`/`toString`) surviving the fold — the task-034 lesson kept out of the new module. 🔑 The engine boundary pair (0 vs missing) is **not** here: it is task 037, in the engine suite, and must not be written twice.
lib/seed-policy.test.ts	8	# ใบ 040 · what `prisma/seed.ts` is allowed to assert about the database in front of it. The load-bearing arm is `seedMode({marked: false, staffCount: 7}) === "adopt"` — the deploy host's first post-fix deploy, where reading "no mark" as "fresh" plants the reference fixture over the only database with real money in it, i.e. this card's bug shipped once by its own fix. The other seven: the empty-database arm, the mark winning over the witness in both directions (including `staffCount: 0`, so a database whose staff all vanished is still not re-planted), a whole-object compare of `plantsFixture`/`recordsMark` over all three modes, and four on `seedReport` — the exact two-line output the db stage greps (`seed: mode=already-initialized created=0`), the planting run never claiming rates were withheld, the created-key line carrying each key's default value, and 🔴 **the omission**: no mode may report a missing `TeachRate` cell. That last arm is the most likely well-meant regression (the rate matrix is the owner's; listing its holes every deploy is an invitation to re-plant them) and it is pinned by whole-array comparison, because an added line is green against every membership assertion. ⚠️ No arm here touches a database — that the seed really withheld the fixture is layer 2, the second seed run in `scripts/check-code.sh`.
lib/gymmo.test.ts	19	# ใบ 058 · ตัวอ่าน worklog ของ Gymmo (วันเวลา · ชนิด · จำนวน · แถวที่อ่านไม่ออก) · **16→18 task 064**, and the pin is why the re-shaping is safe: `GymmoParse.problems` became `GymmoReadProblem[]` (`{sheetName, rowText, rawWhen, date, reason}`) because a problem now has to be *stored* and therefore *keyed*, so every assertion that used to read the rendered sentence with `toContain` now reads a field. Re-shaping assertions cannot move this number (it counts `tests - skipped`) — only the two added arms can, which is exactly the direction that must be deliberate. (+1: the rendered sentence stays **byte-identical** — `` `${readProblemLabel(p)}: ${p.reason}` `` equals what this module emitted before 064, which is what let `gymmoParseProblem`'s split-on-the-first-`": "` hack be **deleted** rather than replaced · +1: 🔴 a blank `#` cell keeps `rowText: ""` and a missing header keeps `rowText: null`, i.e. a row problem and a whole-sheet problem stay two different things. `rowText` is the `#` cell **verbatim** because it is half of the stored key; the `"?"` default belongs to the human-facing label only. counter-test 2026-09-21: `cells[0] ?? ""` → `cells[0] || "?"` kills that arm and nothing else — a blank-`#` row would then key as `["ชีต", "?", …]`, colliding with any literal `?` row at the same time and silently merging two reasons into one stored row) · **18→19 ใบ 064 fix round**: `parseGymmoWhen` runs **first**, so three of the four row rejects already know their date and only an unreadable `Date & Time` genuinely does not. Storing `null` for all four put a perfectly dated August reject into **every** period’s blocker count — and a `kind: "row"` problem has **no clearing path at all** (its key is the 3-tuple `[sheet, #, rawWhen]` while the repaired คาบ is written under a 4-tuple `sourceKey`), so that is permanent until ใบ 066 answers. A blocker count stuck at a constant is a count people learn to ignore, and the count it dies beside is the one carrying ประพัฒน์’s 8,000 ฿. The arm asserts all four branches at once — `Type`/`Students`/`Class` carry `2026-09-04`, the unreadable date carries `null` — plus `rawWhen` being carried verbatim whether or not it parsed. ⚠️ **19 = 16 literal `test(` + one 3-row `test.each`** — a naive grep reads 16; do not "correct" this number downward
lib/gymmo-map.test.ts	15	# ใบ 058 · รอยต่อชื่อระหว่าง Gymmo กับฐานข้อมูล (ชื่อคลาส/ชื่อเทรนเนอร์/คนที่ออกแล้ว)
lib/gymmo-import.test.ts	24	# ใบ 063 · แผนการเขียน `ClassSession` จากไฟล์ Gymmo — หกอาร์มบน `sourceKey` (ถอดกลับเป็น 4-tuple ได้ = injective · คู่ที่ `|`-join ชนกันแต่ตัวนี้แยกได้ ซึ่งคือคลาสบั๊กเดียวกับ ใบ 035 · `"`/`,`/`\` ในชื่อคลาส · ครูคนเดียวสอนคลาสเดียวกันสองรอบในวันเดียว = สองคีย์ ตามข้อ ⛔ ของใบ · ยอดคนไม่อยู่ในคีย์ ⇒ แก้ยอดย้อนหลังแล้ว **อัปเดต** ไม่ใช่เพิ่มแถว · 🔴 **ทุกการสะกดที่ `matchTrainer` จับคู่เป็นคนเดียวกันต้องได้คีย์เดียว** — คีย์ผ่าน `normalizeTrainer(readTrainerSheet(...).name)` ตัวเดียวกับชั้นที่ตัดสินว่า *นี่คือใคร* (ใบ 063 รอบแก้ที่สอง จาก `payroll-auditor`): Gymmo **เปลี่ยนชื่อชีตของครูที่ลาออก** (`(Deleted)`) และชื่อบัญชีถูกพิมพ์ใหม่เป็น `PT ธันยา มูลละคร` หรือมีช่องว่างเกินได้ · ความต่างที่ชั้นจับคู่ยกให้แต่คีย์ไม่ยก = **ซ้ำแบบเงียบ** เพราะจับคู่ยังสำเร็จ ⇒ ไม่มีอะไรเข้า `problems` แต่ทุกแถวได้คีย์ใหม่ `diffGymmoPlan` อ่านว่า `create` ⇒ 24 คาบเดือน ส.ค. ของธันยาเกิดใหม่ทั้งชุด มูลค่า 8,050 → 16,100 · `classPay` **3,050 → 11,100 บาทในเดือนเดียว** และซ้ำอีกทุกเดือนที่สลิปยังเป็น draft · §2 ข้อ 6: identity ห้ามใช้กฎที่แคบกว่า resolution ⇒ **คีย์ที่เก็บไม่ใช่ชื่อสำหรับแสดง** และการแก้ `normalizeTrainer` จะ re-key ทุกแถว) · เก้าอาร์มบน `planGymmoImport` (แถวปกติ · **ยอดติดลบเขียนตามจริงไม่ clamp** ซึ่งเป็นสิ่งที่ยาม ใบ 025 ใน `lib/payroll.ts` ต้องเห็น · แถว PT นับแล้วไม่นำเข้าและไม่ใช่ปัญหา · ครูไม่รู้จัก/คลาสไม่มีราคา เข้า problems ทีละแถวโดยไฟล์ที่เหลือยังนำเข้าได้ พร้อมข้อความไทยของ `lib/gymmo-map.ts` เป๊ะ ๆ · ชื่อพ้องเขียน id ของชื่อในระบบแต่คีย์เก็บชื่อดิบ · 🔴 **สองแถวในไฟล์เดียวที่คีย์ตรงกัน → problems ทั้งคู่ ไม่เขียนสักแถว** เพราะ last-write-wins คือการเลือกยอดคนแทนคนโดยไม่บอก = §2 ข้อ 4 · ไฟล์ว่างเทียบทั้งก้อน · **problems ของตัวอ่านถูกพาเข้ามาด้วยและอยู่หน้าสุด** — `planGymmoImport` รับ `GymmoParse` ทั้งก้อนไม่ใช่แค่ `rows` เพื่อให้ผู้เรียกทิ้งแถวที่ `lib/gymmo.ts` อ่านไม่ออกไม่ได้) · สามอาร์มบน `diffGymmoPlan` (**นำเข้าไฟล์เดิมซ้ำ = ไม่เขียนอะไรเลย** ซึ่งคือเทสที่ §4 ของใบบอกว่าถ้าไม่มีใบนี้ยังไม่เสร็จ · ยอดที่แก้แล้วเป็น update ส่วนคาบใหม่เป็น create · **เดือนถัดจากที่ครูลาออก ไฟล์เดิมต้องเป็น `unchanged` ไม่ใช่ `create`** ซึ่งคือครึ่งที่เป็นเงินของอาร์ม `(Deleted)` ข้างบน) · หนึ่งอาร์มบน `gymmoPlanRange` · และหนึ่งอาร์มปลายทาง: 24 คาบสังเคราะห์ของ ธันยา ส.ค. → 8,050 − โควต้า 5,000 = **3,050** ผ่าน `computePayslip` จริงโดยไม่มีคำเตือน ⚠️ **สังเคราะห์ ไม่ใช่ไฟล์จริง** (export ของ Gymmo ไม่ได้อยู่ในรีโป) ⇒ อาร์มนี้พิสูจน์รอยต่อ import→engine ไม่ได้พิสูจน์ไฟล์ ม.ค.–ก.ย. · counter-test 2026-09-21: เปลี่ยน JSON เป็น `join("|")` ฆ่า 3 อาร์ม (คู่ที่ชนกันคืออาร์มที่กั้นคลาสบั๊กนี้ไว้) · counter-test 2026-09-21 รอบสอง: คีย์ด้วย `row.trainerSheet` ดิบ ๆ ฆ่า 2 อาร์ม (คีย์ `(Deleted)` กับครึ่งที่เป็นเงินของมัน) · counter-test 2026-09-21 รอบสาม: ถอด `normalizeTrainer` ออกจากคีย์ (เหลือแค่ `readTrainerSheet`) ฆ่า 2 อาร์ม · **20→22 ใบ 064**: ทุก `GymmoProblem` ต้องพา `key` ที่ไม่ว่างมาด้วย และคีย์ของแถวที่ถูกปฏิเสธต้องเป็น `gymmoSourceKey(row)` **ของฟังก์ชันนั้นเอง** (อาร์มเดินฉากจริง: `Pilates Flow` ยังไม่มีราคา ⇒ เป็นปัญหา · เพิ่มราคาแล้วนำเข้าไฟล์เดิมซ้ำ ⇒ แถวเดิมเขียนได้ **ใต้คีย์เดียวกัน** ⇒ `deleteMany` ที่คีย์นั้นล้างแถวปัญหาในทรานแซกชันเดียวกัน) · และอาร์มที่สองปักคอลัมน์ที่คิวต้องใช้: `date`/`className`/`timeText` + `trainerSheet` ที่เป็น**การสะกดของไฟล์** ไม่ใช่ครึ่งที่ normalize แล้วของคีย์ · ส่วนปัญหาของตัวอ่านต้องเป็น `kind: "row"` กับ `date: null` ซึ่งเป็นเหตุผลที่มันนับอยู่ในทุกงวดและไม่มีการนำเข้าครั้งไหนล้างได้ (ใบ 066) · counter-test 2026-09-21 (รอบแก้ · ทรีสะอาด · `restore` เทียบ sha256 ผ่าน · `git status` ตรงกับก่อนเริ่มเป๊ะ): เขียนคีย์ของ `sessionRef` ขึ้นมาใหม่จาก `row.trainerSheet` ดิบ ๆ แทนการเรียก `gymmoSourceKey` ⇒ **แดง 8 อาร์ม ชื่อเต็ม**: planGymmoImport > a class row becomes one write, both counts verbatim · planGymmoImport > a class with no price is one problem… · planGymmoImport > a refused row is keyed by `gymmoSourceKey` itself… · gymmoProblemRows > a session key is byte-identical to gymmoSourceKey… · gymmoProblemRows > trainerSheet is the file's spelling… · gymmoProblemRows > row order is first appearance… · gymmoProblemRows > a write landing in a closed period is a problem again… · gymmoProblemRows > an unchanged write in a closed period is not a problem… · 3 อาร์มแรกอยู่ไฟล์นี้ อีก 5 อยู่ `lib/class-problems.test.ts` ⚠️ รอบแรกบันทึกไว้ 4 อาร์ม ซึ่ง `payroll-auditor` ไม่รับ เพราะ `restore` ถูกใช้ซ้ำจน sha256 ไม่ได้เทียบจริง — เลข 8 นี้คือตัวที่พิสูจน์แล้ว (7 ตอนกลางรอบ ก่อนอาร์ม `unchanged` จะถูกเพิ่ม) (+2 ใบ 065 รอบ 5: `decodeGymmoSourceKey`, round-tripped against **the builder** rather than a literal, plus the not-a-4-tuple ⇒ `null` arm. `/classes` shows the decoded clock so an admin can tell 18:00 from 18:30 before deleting one of two คาบ that differ in no other rendered cell — a decoder drifted from `gymmoSourceKey` prints the wrong field confidently, and deleting the wrong half is 200 ฿/month restored by the next upload under a two-click confirmation saying it was fixed)
lib/class-problems.test.ts	10	# task 064 · the identity and the storage shape of a Gymmo row that did NOT become a `ClassSession`. 🔴 **The highest-value arm is "a session key is byte-identical to `gymmoSourceKey`"**: clearing is a keyed `deleteMany` over the very `sourceKey`s the import writes, so those two strings must come from **one** function. One byte of drift and the delete never matches — a คาบ whose `ClassPrice`/`TrainerAlias` has since been added stays in the queue for ever and `/payslips` blocks a period that is in fact clean, i.e. the card ships and does nothing. The arm walks the real fix (add the alias, re-plan, assert the write key equals the stored key) rather than comparing two literals. 🔴 **Second: the merge.** The in-file duplicate rule emits **two** problems for **one** `sourceKey`, and two reader rejects sharing **all three** reader-key cells (same sheet, same `#`, same raw `Date & Time`) key alike — ⚠️ narrower than before the key became arity 3, where a blank `#` alone sufficed, so the duplicate rule is what keeps this merge load-bearing rather than theoretical ⇒ without the pure dedupe `createMany` throws P2002 and the **whole import transaction aborts**, so a file that imported yesterday fails on every attempt today because one row in it is duplicated; dropping the second instead would throw away half the evidence (§2 rule 4 one level down). The remaining four: the three key shapes kept apart by **arity** (4/3/1, asserted by `JSON.parse` round-trip — ใบ 035's bug class, and why no `a|b|c` join) · a blank `#` keying stably as a *row* and not as its sheet · `trainerSheet` being the **file's** spelling while the key half is `ธันยามูลละคร` (the key is not a display name) · and deterministic row order, since these rows go into one `createMany`. ⚠️ **No baht anywhere in this file, by construction** (§2 rules 2–3): an amount for these คาบ could only come from `computePayslip` over `ClassSession` rows, which by definition do not exist for these keys. ⚠️ **The transaction itself is reviewed, not pinned** — no test here reaches a database (`.docs/knowledge/ops/gate-tiers-and-pins.md`), so `applyGymmoImport`'s `!writes.length && !problems.length` guard is checked by eye. counter-test 2026-09-21: dropping the merge (last-problem-wins) kills 2 arms · dropping the dedupe entirely kills 3 · re-deriving the session key without `normalizeTrainer` kills 2 · **6→9 ใบ 064 fix round**, all three from `payroll-auditor`: (+1) 🔴 **a write landing in a period whose slip is already closed is a problem again, under the same key** — and keyed by **(staffId, period)**, pinned by an arm asserting that a *different* staff member's closed slip in the very same month adds nothing (an admin approves slips one at a time, so that is the common case; a period-level test would flag every คาบ of the month, including ones the next run really will pay) — `runPayroll` refuses to recompute a non-draft slip and no screen reopens one, so that คาบ is never paid by anybody; measured on ประพัฒน์ (`baseSalary: 0`, `classCredit: 0`), 20 `Pilates Flow` คาบ in 08/2026 ⇒ `classPay = max(0, 8000 − 0) = 8,000 ฿`, his **entire month**, and the first cut deleted all twenty problem rows on the very import that wrote them, leaving both `/payslips` counts reading 0. The arm pins the negative direction too (no closed slip, and a closed slip in a *different* period ⇒ nothing extra), that the key equals the write key (so delete-then-insert **replaces** the stale reason instead of colliding), and that the reason names the period, the real status, the way out and ใบ 013 ข้อ 2 · (+1) **`#` is not an identity across exports** — it restarts at 1 in every Gymmo file, so row 14 of the August file and row 14 of the September file must be two keys; keyed on `[sheet, "14"]` alone the second upload would `deleteMany` the first’s still-true problem and store an unrelated row’s reason under it, which is T3’s silent loss through a key that is not an identity rather than through a date range. The arm also pins that both rows survive one write **and** that their human-facing `rowLabel` is identical, which is exactly why the key may not be · (+1) **an in-file duplicate is `kind: "duplicate"`, not `"session"`** — the stored คาบ may be there at the **wrong amount** (booked 5/noShow 3 ⇒ attended 2 ⇒ half of 400 = 200 ฿, against the file’s booked 9/noShow 0 ⇒ 400 ฿), so its existence proves nothing and `pendingClassImportInPeriod` must not exclude it; `kind` is the only thing carrying that distinction into the count · **9→10 ใบ 064 fix round 2** — 🔴 **the other direction of the same question, and the one that survived two reviews**: `unchanged` means **nothing was written** (the คาบ was already in `ClassSession`), so a closed slip beside it is overwhelmingly likely to be the slip that **paid** it. Measured: ธันยา (`โอ`, `classCredit: 5000`), 08/2026, 24 คาบ, class value 8,050 ⇒ `classPay = 3,050 ฿`, slip `paid` · re-uploading the Jan–Sep workbook in October to pick up September (the flow this module's header mandates) makes August `created: 0, updated: 0, unchanged: 24`, and the previous cut manufactured **24 rows claiming those คาบ were ยังไม่ถูกจ่าย** — five paid months ⇒ five permanently non-zero false counts, with ประพัฒน์'s twenty genuinely unpaid 8,000 ฿ rows indistinguishable among them. ⚠️ The arm pins `leaveAlone`, not merely "no problem": the key must also leave the caller's **delete set**, or a *true* closed-slip row an earlier import recorded is deleted and not re-inserted — a false positive traded for a **false negative, in the money direction**. It also pins that `unchanged` + an **open** slip is NOT left alone (its stale reason must still clear) and that the two output halves are disjoint. 🔑 The whole four-quadrant decision was moved out of the transaction into the pure `closedSlipOutcome` **because** this defect survived two reviews while it lived in the reviewed-not-pinned zone. counter-test 2026-09-21 (รอบแก้ · ทรีสะอาด · `restore` เทียบ sha256 ผ่าน · `git status` ตรงกับก่อนเริ่มเป๊ะ): เขียนคีย์ของ `sessionRef` ขึ้นมาใหม่จาก `row.trainerSheet` ดิบ ๆ แทนการเรียก `gymmoSourceKey` ⇒ **แดง 8 อาร์ม** (3 ที่ `lib/gymmo-import.test.ts` · 5 ที่นี่) ⚠️ เลข "ฆ่า 2/3/2 อาร์ม" ข้างบนเป็นของรอบแรกก่อนจะมีอาร์มเหล่านี้
lib/gymmo-hand-keyed.test.ts	5	# ใบ 065 · which hand-keyed คาบ an upload is about to duplicate, matched on (UTC day, staffId, classId). 🔴 **The arm that earns its keep is the `|`-collision one** — `2026-08-04|"s-o|c-core"|"x"` and `2026-08-04|"s-o"|"c-core|x"` are different triples with one joined key, and a false duplicate here makes a human delete a real คาบ (the ใบ 035 bug class, on a new key). The `fileRows: 2` arm is the 07:15+09:00 pair that `ClassSession`'s clock-less date collapses. Counter-tested: a `|` join kills the first, a hardcoded 1 kills the second
lib/class-problems-copy.test.ts	7	# ใบ 065 รอบ 2 · the Thai a reader of the import queue acts on, as a pure `(kind, state)` grid. 🔴 **This file exists because the same defect BLOCKed twice and no gate could see it**: the exit text lived in a `.tsx` string, so round 1 (a closed-slip คาบ told to re-import, which `leaveAlone` guarantees will not clear it, beside a claim it is not in the database ⇒ ประพัฒน์'s 8,000 ฿ keyed in again by hand) and round 2 (the same shape through `kind: "duplicate"`, whose คาบ may exist at the wrong head count ⇒ 200 + 400 = 600 ฿ for one 400 ฿ คาบ) were both reachable only by reading. One arm walks every cell of the 3×4 grid, one asserts `problemIsCleared` is true for **exactly one** of the twelve (round 3: the count and the screen each had their own copy of that predicate and disagreed about `duplicate` + `accountedFor`), and the rest pin the cells that cost money. Lowering this number means a cell stopped being checked
