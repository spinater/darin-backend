#!/usr/bin/env bash
# ย้ายใบงานระหว่าง **สามบ้าน** `tasks/todo/` · `tasks/done/` · `tasks/todo-human/`
# **พร้อมกวาดลิงก์ทุกเส้นที่ชี้มาหามัน** (task 062 · บ้านที่สามเพิ่มที่ task 231)
#
# ## ปัญหาที่ปิด
# ทุกครั้งที่ใบถูกย้าย ลิงก์ที่ **ไฟล์อื่น** ชี้มาหามันจะพัง และความแดงไปโผล่กับคนที่รันเกต
# *ถัดไป* ไม่ใช่กับ change ที่ทำให้เกิด · เกิดสี่ครั้งในวันเดียว (2026-08-13) และ **ครั้งที่หก
# เกิด 2026-08-27** ตอนย้ายใบ 143 เข้า `done/` แล้วใบ 141 ที่ชี้มาหามันพัง — จับได้เพราะใบ
# ถัดไปบังเอิญรัน verify พอดี ⇒ กติกา "รัน check-links เองสิ" **ช่วยให้จับได้ แต่ไม่ได้ป้องกัน**
# ปัญหาอยู่ที่กลไก ไม่ใช่ที่วินัย
#
# ## สี่รูปของลิงก์ที่ต้องกวาด — **นับจากของจริงในรีโป ไม่ใช่เดา**
#   1. `…/tasks/todo/NNN-slug.md`  (ข้ามจาก `.docs/**` — prefix อะไรก็ได้)
#   2. `../todo/NNN-slug.md`        (ระหว่าง `tasks/done/` ↔ `tasks/todo/`) — 15 + 45 เส้น
#   3. `NNN-slug.md` เปล่า ๆ        (ไดเรกทอรีเดียวกัน) — 36 เส้น
#   4. `todo/NNN-slug.md`           (จากไฟล์ที่อยู่ใน `tasks/` เอง: README · HANDOFF-* · PLAN-*)
#      — 5 บรรทัด / 6 จุด ใน 5 ไฟล์ (วัด 2026-09-06 ใบ 232) · **รูปนี้ตกสำรวจมาตั้งแต่ใบ 062**
#      และเจอเพราะย้ายใบจริงครั้งแรกด้วย `--human` แล้วเกตแดง ไม่ใช่เพราะอ่านโค้ดเจอ
# และรูปที่ **ใบงานเองเป็นฝ่ายชี้ออก**: ลิงก์เปล่า ๆ ของไฟล์ที่ย้าย จะพังทันทีที่มันเปลี่ยน
# ไดเรกทอรี ถ้าปลายทางยังอยู่ที่เดิม ⇒ ต้องกวาด**ขาออกของตัวเอง**ด้วย ไม่ใช่แค่ขาเข้า
#
# ## ไฟล์โค้ดก็ต้องกวาด ไม่ใช่แค่ `.md` (task 168)
# พอยน์เตอร์ `tasks/<บ้าน>/NNN-slug.md` ที่อยู่ในคอมเมนต์ของไฟล์ **ที่ไม่ใช่ `.md` ทุกชนิด**
# (เลือกด้วยการ **ตัดออกด้วย `:!`** ไม่ใช่ระบุนามสกุลที่จะกวาด — เหตุผลเต็มอยู่ที่แขน `:!`
# ของชั้นที่สามใน `scripts/check-links.sh` · **อ้างด้วยตัวสัญลักษณ์ ไม่ใช่เลขบรรทัด** (§2 rule 3):
# บรรทัดนี้เคยเขียนว่า `check-links.sh:65` แล้วเน่าไป ~110 บรรทัด — วันนี้ 65 อยู่กลางข้อความ FAIL
# ของชั้นที่สอง ซึ่งคนละเรื่องกันคนละชั้น)
# เน่าด้วยเหตุผลเดียวกันเป๊ะ และก่อนใบ 168 **ไม่มีใครกวาดและไม่มีด่านไหนเห็น** ⇒ ต้องกวาดที่นี่
# ไม่งั้นชั้นที่สามของ `check-links.sh` จะไปแดงใส่คนที่ย้ายใบ *ถัดไป* ซึ่งคืออาการเดียวกับที่ใบ 062
# ตั้งใจปิด · ในโค้ดมีแค่ **รูปที่ 1** (พาธเต็มจากรากรีโป) — รูปที่ 2/3 เป็นรูปสัมพัทธ์ของไฟล์
# ใน `tasks/` เอง ⇒ ปล่อยให้ sed ตัวเดิมทำงานทั้งชุด ไม่ต้องแยกทาง
# **ยกเว้น `prisma/migrations/**`**: แก้ไฟล์ที่ apply แล้วไม่ได้ (เทียบ checksum) ⇒ ข้ามไป
# ซึ่งไม่ทำให้ด่านแดง เพราะด่านก็ยกเว้นไฟล์กลุ่มเดียวกันด้วยเหตุผลเดียวกัน
#
# ## มันต้อง `git add` สิ่งที่มันแก้ให้ด้วย ไม่ใช่แค่แก้ (task 276)
# เดิมมัน `git add` ให้เฉพาะสิ่งที่ `git mv` ทำ ⇒ ไฟล์ที่มันไป **แก้เนื้อ** ให้ (การ์ดความรู้ ·
# ไฟล์โค้ด · README) ถูกทิ้งไว้ใน working tree เฉย ๆ ⇒ คนที่ `git add` ด้วยพาธแคบ
# (`git add -A tasks`) ได้คอมมิตที่ย้ายใบแล้วแต่ลิงก์ยังชี้บ้านเก่า ⇒ **`check-links.sh` เขียว
# บนเครื่องคนย้าย (working tree มีการแก้อยู่) แต่แดงที่ HEAD** — เขียวตรงที่เราดู แดงตรงที่คนอื่นเห็น
# และความแดงไปลงกับคนที่รันเกตถัดไป ซึ่งคือความไม่สมมาตรที่ใบ 062 เปิดมาปิดพอดี
# · เกิดจริง **อย่างน้อยสี่ครั้ง ด้วยคำสั่งคนละคำสั่ง** — ปิดใบ 222 (2026-09-05,
# api/src/patient_device/transfers/receive.rs ยังชี้ `todo/`) แล้ว `a1aeaed6` · `4be9e09c`
# · `8809cb4d` (สามใบหลังมี SHA กำกับ ใบแรกเจอจากบันทึกของสายที่เจอตอนทำใบ 276)
# ⇒ ไม่ใช่ความสะเพร่าของคำสั่งใดคำสั่งหนึ่ง · และ **กติกาที่ต้องจำ คือสิ่งที่เครื่องมือมีไว้แทน**
#
# 🔑 **แต่ stage ให้ทุกไฟล์ไม่ได้** — ทรีที่มีงานค้างอยู่คือกรณีปกติของรีโปนี้ และ §5/§6 rule 4
# บังคับให้บางคอมมิตต้องแยก stage (คอมมิตแตกการ์ดห้ามมีซอร์ส · คอมมิตแตกไฟล์ห้ามมีงานจริง)
# ⇒ เส้นแบ่งคือ **"ไฟล์นั้นมีงานค้างที่ยังไม่ stage อยู่ก่อนไหม"** — ไม่ใช่ "สะอาดทั้งใบ":
# ของที่คนตั้งใจ `git add` ไว้เองแล้ว ถือว่าเรียบร้อย (ไม่งั้นจะกันท่าปกติ "สเตจการ์ดของตัวเอง
# แล้วค่อยย้ายใบ" ทิ้งไปเฉย ๆ) ⇒ ไม่มีงานค้างที่ยังไม่ stage ⇒ **สิ่งเดียวที่เครื่องมือเพิ่มเข้า
# index คือสิ่งที่มันเขียนเอง** ⇒ `git add` ให้เลย · มีงานค้างอยู่ก่อน / ยังไม่ถูก track /
# **เป็น symlink** ⇒ **ไม่แตะ แล้วบอกชื่อมันออกมาดัง ๆ** — รูปเดียวกับที่มันรายงานหาง `- status:`
# และบรรทัด `- 🚫` (สิ่งที่เครื่องตัดสินได้ มันทำ · สิ่งที่ตัดสินแทนคนไม่ได้ มันตั้งชื่อแล้วปล่อยให้คนแก้)
#
# ⚠️ **และมันไม่เชื่อบัญชีของตัวเองอย่างเดียว** — ท้ายรอบมันถาม `git diff --name-only` ว่ายังมี
# อะไรค้างที่ไม่มีชื่อในบัญชีไหม · ด่านนี้ไม่ได้มีไว้เผื่อ: **symlink ทำให้เกิดจริง** (git ตอบเรื่อง
# ตัวลิงก์ แต่ `cat >` เขียนทะลุไปไฟล์ปลายทาง ⇒ ของจริงเปลี่ยน ไม่ถูก stage และไม่มีใครเอ่ยชื่อ
# = บั๊กใบนี้กลับมาทางที่สองในทิศที่เงียบ) — เคส AA/ความไว 12 ของ selftest ตรึงไว้แล้ว
#
# ## ล้มแล้วต้อง **บอกว่าล้มตรงไหน** ไม่ใช่ตายเงียบกลางการกวาด (ใบ 284)
# `sed`/`cat` ใน `edit()` เคยไม่มีใครอ่าน rc ⇒ ล้มเมื่อไรสคริปต์ตายตรงนั้น **หลัง `git mv` ไปแล้ว**
# = ใบย้ายแล้วแต่ลิงก์กวาดไปครึ่งเดียว และ `$work` ค้างเพราะไม่เคยถึง `rmdir` ⇒ **ทรีที่แย่กว่า
# ตอนเริ่ม** เพราะคนที่มาเจอไม่รู้ว่ามันหยุดตรงไหน · วันนี้: อ่าน rc เอง → **กวาดต่อจนจบทั้งชุด**
# → กอง ⛔ ท้ายรอบเรียกชื่อไฟล์ · สาเหตุจริง · และทางออกที่พิมพ์ตามได้ · `trap` เก็บ `$work`
# ทุกทางออก · และ **⛔ ไม่ว่าง = exit ไม่เป็นศูนย์ แม้เกตจะเขียว** — สัญญาเดียวกับหาง `- status:`
# และบรรทัด `- 🚫`: สิ่งที่เครื่องตัดสินได้ มันทำ · สิ่งที่ตัดสินแทนคนไม่ได้ มันตั้งชื่อแล้วปล่อยให้คนแก้
#
# ## `git mv` เสมอ ห้ามก๊อปแล้วลบ
# ครั้งที่ห้าของบั๊กนี้คือไฟล์ถูก**ก๊อป** ⇒ ใบเดียวอยู่สองบ้าน คิวงานโกหกว่ายังเปิดอยู่ และ
# **ไม่มี checker ตัวไหนเห็น** · ด่านนั้นอยู่ที่ `check-links.sh` แล้ว (ใบ 062 เหมือนกัน)
set -euo pipefail
cd "$(dirname "$0")/.."

usage() {
  cat >&2 <<'EOF'
ใช้: bash scripts/task-move.sh NNN [--reopen|--human|--answered]   (NNN = เลขใบสามหลัก)
  (เปล่า)      todo        → done          ใบ ship แล้ว
  --reopen     done        → todo          เปิดใบเก่ากลับมา
  --human      todo        → todo-human    บล็อกที่คน ไม่ใช่ที่โค้ด (task 136)
  --answered   todo-human  → todo          คนตอบแล้ว ย้ายกลับทันที
EOF
  exit 2
}

num="${1:-}"
case "$num" in
  [0-9][0-9][0-9]) ;;
  *) usage ;;
esac
# **บ้านที่สามด้วย ไม่ใช่สองบ้าน** (task 231) — `todo-human/` มีมาตั้งแต่ใบ 136 แต่เครื่องมือรู้จัก
# แค่ `todo`↔`done` ⇒ ใบที่เข้า–ออกบ้านนั้นถูกย้ายด้วยมือมาตลอด ซึ่งคือปัญหาที่ใบ 062 เปิดมาปิดพอดี
# · ตรรกะการกวาดข้างล่างเป็น generic ใน `$from`/`$to` อยู่แล้วทั้งใบ ⇒ ที่เพิ่มคือ**ทางเข้า**
#
# ⚠️ และเป็น `case` ที่มีแขน `*)` ไม่ใช่ `if [ "$2" = "--reopen" ]` อย่างเดิม: แบบเดิม**กลืนแฟล็ก
# ที่พิมพ์ผิดเงียบ ๆ** แล้วเดินต่อด้วยค่า default ⇒ `task-move.sh 109 --humen` = ย้ายใบที่ยังไม่เสร็จ
# เข้า `done/` พร้อมกวาดลิงก์ทั้งรีโป แล้วจบด้วย `check-links: OK` — ผลที่ผิดสนิทแต่หน้าตาเหมือน
# ความสำเร็จทุกประการ · อาร์กิวเมนต์เกินก็ถูกกลืนแบบเดียวกัน ⇒ ตรวจจำนวนด้วย
[ "$#" -le 2 ] || usage
# `rev_flag`/`fwd_flag` = คู่แฟล็ก **ขากลับ/ขาไป** ของรอบนี้ ใช้ตอนพิมพ์ทางออกให้กอง ⛔ (ใบ 284)
# ⇒ คำสั่งที่พิมพ์ตามได้จริง ไม่ใช่ "ลองย้ายกลับดู" · 🔑 **อยู่ในแขนเดียวกับ `$from`/`$to`
# ไม่ใช่ `case` ที่สอง** — มันคือตารางสี่แถวเดียวกัน ⇒ แยกเป็นสอง `case` เมื่อไรก็คือ **สองบ้าน
# ของคำตัดสินเดียว** ที่ §4 ห้ามไว้ ("ก่อนใบ 193 มันมีสองสำเนา และสองสำเนานั้นบังเอิญตรงกันเฉย ๆ")
# · และแขน `*)` ของสำเนาที่สองดักได้แค่ **คู่ที่หายไป** ไม่มีวันดักคู่ที่ **แมปผิด** ⇒ สลับค่า
# สองแถวแล้วไม่มีอะไรแดง ขณะที่เครื่องมือพิมพ์คำสั่งที่ย้ายใบไปผิดทาง (รอบรีวิวของใบ 284 จับได้)
from="todo"; to="done"; rev_flag=" --reopen"; fwd_flag=""
case "${2:-}" in
  "")         ;;
  --reopen)   from="done";       to="todo";       rev_flag="";            fwd_flag=" --reopen" ;;
  --human)    from="todo";       to="todo-human"; rev_flag=" --answered"; fwd_flag=" --human" ;;
  --answered) from="todo-human"; to="todo";       rev_flag=" --human";    fwd_flag=" --answered" ;;
  *) usage ;;
esac

# หาไฟล์ **ใบเดียวเท่านั้น** — สองใบที่ขึ้นต้นเลขเดียวกันคือความกำกวมที่ห้ามเดาแทนคน
found=""
for f in "tasks/$from/$num"-*.md; do
  [ -e "$f" ] || continue
  [ -n "$found" ] && { echo "task-move: มีมากกว่าหนึ่งไฟล์ที่ขึ้นต้น $num ใน tasks/$from/" >&2; exit 1; }
  found="$f"
done
[ -n "$found" ] || { echo "task-move: ไม่พบใบ $num ใน tasks/$from/" >&2; exit 1; }
slug="$(basename "$found")"
esc="${slug//./\\.}"

# สภาพของทรี **ก่อน** แตะอะไรเลย — ใช้ทานท้ายรอบว่ามีไฟล์ไหนเปลี่ยนไปโดยไม่มีใครบันทึกชื่อไว้
dirty_before=()
# 🔴 **`-z` ของสองจุดนี้ต้องมาคู่กันเสมอ** (ใบ 314) — กองนี้ถูกเทียบกับ `git diff` ท้ายรอบ
# ด้วย `grep -qxF` ⇒ ก่อนใบ 314 มันถูกต้อง **เพราะสองฝั่ง quote เหมือนกัน** คือบั๊กสองตัวหักล้างกัน
# ไม่ใช่เพราะโค้ดถูก ⇒ แปลงข้างเดียวเมื่อไร ไฟล์ชื่อไทยที่ค้างอยู่ก่อนจะตกไปกอง "ไม่มีใครอธิบาย"
# = แดงปลอมที่เรียกชื่อสาเหตุผิด ซึ่งแพงกว่าไม่มีแดง
while IFS= read -r -d '' f; do dirty_before+=("$f"); done < <(git diff -z --name-only; git ls-files -o --exclude-standard -z)
# ใบที่มีงานค้างอยู่ก่อนย้าย = พาธ**ใหม่**ของมันก็ค้างอยู่ก่อนเหมือนกัน (ไม่ใช่ของที่เครื่องมือทำ)
git diff --quiet -- "$found" 2>/dev/null || dirty_before+=("tasks/$to/$slug")

# 🔴 **เก็บ `$work` ให้เสมอ ไม่ว่าจะออกทางไหน** (ใบ 284) — เดิมมีแต่บรรทัด `rmdir` ท้ายรอบ ⇒ รอบที่
# ตายกลางทาง (หรือถูก Ctrl-C) ทิ้งไดเรกทอรีค้างไว้ทุกครั้ง และชื่อไม่ซ้ำเพราะต่อ `$$` ⇒ **ค้างสะสม
# โดยไม่มีใครเก็บ** · `trap` **ไม่ได้แทน**บรรทัดท้ายรอบ มัน**ซ้อน**: บรรทัดนั้นต้องลบ *ก่อน*
# `check-links.sh` รัน เพราะในทรีที่ `.scratch/` ไม่ได้ถูก ignore (แซนด์บ็อกซ์ของ selftest เป็นแบบนั้น)
# ไฟล์ใน `$work` จะถูกเกตนับเป็นไฟล์ที่ต้องตรวจ (`git ls-files -o`) ⇒ แดงปลอมที่ชี้สาเหตุผิด
#
# 🔑 **สามบรรทัดนี้อยู่ *ก่อน* `git mv` โดยตั้งใจ** (รอบรีวิวของใบ 284) — ไม่มีอะไรในนั้นพึ่งการย้าย
# และสองคำสั่งแรกก็อยู่ใต้ `set -e` เหมือนกันหมด ⇒ วางไว้หลัง `git mv` เมื่อไร `.scratch/` ที่เขียน
# ไม่ได้จะฆ่ารอบ **ทันทีหลังใบถูกย้าย** ด้วย error ของ `mkdir` เปล่า ๆ ไม่มีกอง ⛔ ไม่มีใครเก็บ
# `$work` = อาการของใบนี้เป๊ะ ๆ ที่กลับมาทางไฟล์ข้างเคียง · วางไว้ก่อน ⇒ ล้มตอนนั้น = **ยังไม่มี
# อะไรถูกย้ายเลย** (ทรีเหมือนเดิมทุกประการ) และ `trap` ยังครอบ `git mv` ที่ล้มเองไปด้วยเป็นของแถม
work=".scratch/task-move-$$"
mkdir -p "$work"
trap 'rm -rf "$work"' EXIT

git mv "$found" "tasks/$to/$slug"
staged=()   # ไฟล์ที่แก้แล้ว `git add` ให้เลย (สะอาดอยู่ก่อนถูกแตะ)
left=()     # ไฟล์ที่แก้แล้ว **จงใจไม่ stage** (มีงานค้างอยู่ก่อน / ยังไม่ถูก track / `git add` ล้ม)
failed=()      # ไฟล์ที่ **เขียนไม่สำเร็จเลย** (ใบ 284) — คนละกองกับ `left` เพราะคนละทางแก้:
failed_why=()  # `left` = แก้แล้วแต่ไม่ stage ให้ (คนกด `git add` เอง) · `failed` = ยังไม่ถูกกวาด

# ตัดสิน **ก่อน** เขียนทับ — หลังเขียนแล้วแยกไม่ออกอีกว่าความต่างมาจากใคร
stageable() { # $1=ไฟล์ · true ก็ต่อเมื่อ track แล้ว **และ** ไม่มี**งานค้างที่ยังไม่ได้ stage** อยู่ก่อน
  # ⚠️ symlink ห้าม auto-stage เด็ดขาด — git ตอบเรื่อง**ตัวลิงก์** แต่ `cat > "$f"` เขียนทะลุไป
  # **ไฟล์ปลายทาง** ⇒ `git add` ตัวลิงก์เป็น no-op (blob ไม่เปลี่ยน) ขณะที่ของจริงถูกเขียนแล้ว
  # ไม่ถูก stage และ **ไม่มีใครเอ่ยชื่อ** = บั๊กของใบนี้เองกลับมาทางที่สอง ในทิศที่เงียบ
  # (รีโปนี้มี symlink ที่ track ไว้ 11 ใบ สามใบเป็น `.md` ใต้ .docs/billing/ ตาม §10)
  [ -L "$1" ] && return 1
  git ls-files --error-unmatch -- "$1" >/dev/null 2>&1 || return 1
  git diff --quiet -- "$1" 2>/dev/null || return 1
}

# ── กองที่ **เครื่องทำแทนคนไม่ได้เลย**: ไฟล์ที่กวาดไม่สำเร็จ (ใบ 284)
# พูดทันทีที่เกิด **และ** เก็บไว้สรุปท้ายรอบ — บรรทัดทันทีบอก *ลำดับ* (กวาดถึงไหนแล้วเจอ)
# ส่วนกองท้ายรอบบอก *ยอด* ซึ่งคือสิ่งที่คนอ่านตอนตัดสินใจว่าจะ `git add` อะไร
note_failed() { # $1=ไฟล์ $2=สาเหตุสั้น ๆ $3=บรรทัดแรกของ stderr จริง (ว่างได้)
  local f="$1" why="$2" msg="${3:-}" seen
  # ตัดคำนำหน้า `<สคริปต์>: line NNN:` ที่ bash ติดมากับข้อความ redirection ทิ้ง — **เลขบรรทัด
  # เน่าทุกครั้งที่มีคนแก้ข้างบน** (§2 rule 3) และสิ่งที่คนอ่านต้องการคือ `<ไฟล์>: Permission denied`
  msg="${msg#*: line *: }"
  [ -z "$msg" ] || why="$why · $msg"
  echo "  ⛔ กวาดไม่สำเร็จ: $f — $why"
  # `edit` ถูกเรียกซ้ำได้หลายรอบต่อไฟล์เดียว (ใบที่ย้ายโดนถึงสี่รอบ) ⇒ กันชื่อซ้ำตั้งแต่ตอนจด
  for seen in ${failed[@]+"${failed[@]}"}; do
    if [ "$seen" = "$f" ]; then return 0; fi
  done
  failed+=("$f"); failed_why+=("$why")
  return 0
}

# 🔴 **ทุกคำสั่งเขียนในนี้อ่าน rc ของตัวเอง แล้ว "กวาดต่อให้จบ" ไม่ใช่ตายคาที่** (ใบ 284)
# เดิม `sed … > "$tmp"` และ `cat "$tmp" > "$f"` ไม่มีใครอ่านสถานะ (ตัวหลังอยู่**ท้าย** `||` list
# ⇒ `set -e` ยังกินมันอยู่) ⇒ ล้มเมื่อไรสคริปต์ตายตรงนั้น **หลัง `git mv` ไปแล้ว** = ทรีที่ใบย้ายแล้ว
# แต่ลิงก์กวาดไปครึ่งเดียว โดยมีแต่ `line NNN: Permission denied` ของ bash เป็นคำอธิบาย ⇒ คนที่มาเจอ
# ไม่มีทางรู้ว่ามันหยุดตรงไหนและเหลืออะไร · **วัดจริงสองทาง** (บน `2be0e225` และ HEAD ของใบ 276):
# ไฟล์ที่ถูกกวาด `chmod 444` · ชื่อไฟล์ที่ขึ้นต้นด้วย `-` (sed กินเป็นแฟล็ก) — ทั้งคู่ได้ผลเดียวกัน
# ⇒ **เลือกทางที่ 2 ของใบ 284: กวาดให้จบทั้งชุดเสมอ แล้วรวบรายงานท้ายรอบ** (สำนวนเดียวกับกอง `left`
# ของใบ 276 และกับ `restore` ของ `counter-test.sh` ที่รายงาน "สิ่งที่ไม่ได้ทำ" — §6 rule 8)
# ⚠️ **ห้ามแก้ด้วยการถอด `set -e`** — `git mv` ที่ล้มต้องหยุดทันที ทั้งไฟล์พึ่งมันอยู่
# ⚠️ และ `edit` **คืน 0 เสมอโดยตั้งใจ** เพราะจุดเรียกทุกจุดเป็นคำสั่งเปล่าในลูป ⇒ คืนค่าไม่ศูนย์
# เมื่อไร `set -e` ก็ฆ่าลูปทั้งลูป = อาการเดิมกลับมาทางประตูหลัง · หลักฐานว่ามันล้มอยู่ที่กอง
# `failed` ซึ่งถูกพิมพ์ท้ายรอบ **และทำให้ exit ไม่เป็นศูนย์** ไม่ได้อยู่ที่ค่าที่ฟังก์ชันคืน
edit() { # $1=ไฟล์ $2..=นิพจน์ sed — เขียนกลับเฉพาะตอนเปลี่ยนจริง (ไม่ปั่น mtime ให้การ์ดเก่า)
  local f="$1"; shift
  # `printf '%s'` ไม่ใช่ `echo` — ชื่อไฟล์ที่ขึ้นต้นด้วย `-` (`-n` · `-e`) ถูก `echo` กินเป็นแฟล็ก
  # ⇒ ได้ชื่อ temp ว่าง = เขียนทับไดเรกทอรี `$work` เอง แล้วล้มด้วยสาเหตุที่เรียกชื่อผิด
  local tmp="$work/$(printf '%s' "$f" | tr '/' '_')"
  local safe=0 err=""
  if stageable "$f"; then safe=1; fi
  # `--` ปิดทางที่ **ชื่อไฟล์กลายเป็นแฟล็ก** ของ `sed`/`cmp` (ทางที่สองของใบ 284) · `2>&1 >"$tmp"`
  # เรียงลำดับนี้เท่านั้น: stderr ไปเข้า `$( )` ก่อน แล้ว stdout ค่อยลงไฟล์ ⇒ ได้ข้อความจริงมาเล่า
  # ⚠️ **ปิดได้เฉพาะฝั่งกวาด ห้ามเขียนว่าปิดครบ**: `check-links.sh` เองยัง `dirname "$f"` และ
  # `grep -oE … "$f"` แบบไม่มี `--` ⇒ ไฟล์ชื่อขึ้นต้น `-` ที่รากรีโปทำให้ **ตัวเกตตาย** (วัดแล้ว:
  # `error: unexpected argument '-d' found`) — ความไม่สมมาตรกลับด้านจากใบ 314 (คราวนั้นเกตเห็น
  # แต่ตัวกวาดเอื้อมไม่ถึง · คราวนี้ตัวกวาดทำได้แต่เกตตาย) ⇒ ยังเป็นงานที่เปิดอยู่ ไม่ใช่ของที่ปิดแล้ว
  if ! err="$(sed "$@" -- "$f" 2>&1 >"$tmp")"; then
    # ⚠️ **ห้ามเดินต่อไป `cmp`** — sed ที่ล้มกลางคันทิ้ง `$tmp` ที่ไม่ครบไว้ ⇒ เขียนทับไฟล์จริง
    # ด้วยผลของคำสั่งที่ล้ม = ทำข้อมูลหาย ซึ่งแพงกว่าการไม่กวาดไฟล์นั้นหลายเท่า
    note_failed "$f" "sed อ่านไฟล์ไม่ได้/นิพจน์ล้ม (ไฟล์ **ไม่ถูกแตะ**)" "${err%%$'\n'*}"
    rm -f -- "$tmp" 2>/dev/null || :
    return 0
  fi
  # **เขียนทับด้วย `cat` ไม่ใช่ `mv`** — `mv` เอา mode ของ temp (0644 ตาม umask) มาด้วย
  # ⇒ ไฟล์ `.sh` ที่เป็น 0755 กลายเป็น 0644 เงียบ ๆ · ก่อนใบ 168 เอื้อมไม่ถึงเพราะกวาดแต่ `.md`
  # (วัดแล้วว่าเกิดจริง: 755 → 644 หลัง `mv`) · `cat >` เขียนในไฟล์เดิม mode เดิมจึงอยู่ครบ
  if ! cmp -s -- "$f" "$tmp"; then
    if ! err="$(cat -- "$tmp" 2>&1 >"$f")"; then
      # ⚠️ ข้อความนี้ **ไม่ได้** พูดว่า "ไฟล์ไม่ถูกแตะ" ต่างจากแขน `sed` ข้างบนโดยตั้งใจ — สองสาเหตุ
      # ใต้แขนนี้จบคนละแบบ: เปิด `>` ไม่ได้ (สิทธิ์) = ไฟล์เดิมอยู่ครบ · เปิดได้แล้ว `cat` ล้มทีหลัง
      # (ดิสก์เต็ม/IO) = **`>` ตัดไฟล์ทิ้งไปแล้วก่อน `cat` จะเริ่มเขียนสักไบต์** ⇒ เนื้อหายจริง
      # ⇒ พูดว่า "อาจ" เพราะเครื่องมือแยกสองอย่างนี้ไม่ออกจาก rc ⇒ เดาแทนคนไม่ได้ (เคส AE)
      note_failed "$f" "เขียนกลับไม่ได้ — **ไฟล์อาจถูกตัดทิ้งไปแล้วบางส่วน** (\`>\` ตัดก่อน \`cat\` เริ่มเขียน · สิทธิ์ไฟล์? ดิสก์เต็ม?)" "${err%%$'\n'*}"
      rm -f -- "$tmp" 2>/dev/null || :
      return 0
    fi
    echo "  แก้ลิงก์: $f"
    # `git add` อยู่ในเงื่อนไขของ `if` ⇒ **แขนนี้แขนเดียว** ล้มแล้วไม่ทำให้ทั้งสคริปต์ตายกลางการกวาด
    # (กวาดครึ่งเดียวแล้วออก = ทรีที่แย่กว่าตอนเริ่ม) · ล้มเมื่อไรก็ไปลงกอง `left` ซึ่งถูกรายงาน
    if [ "$safe" -eq 1 ] && git add -- "$f" 2>/dev/null; then
      staged+=("$f")
    else
      left+=("$f")
    fi
  fi
  # ⚠️ **การเก็บกวาดต้องไม่ใช่สิ่งที่ฆ่ารอบเสียเอง** (ใบ 284) — `rm -f` คืน 1 ให้ไดเรกทอรี
  # (ฉากที่ชื่อ temp เพี้ยนจนกลายเป็นตัว `$work` เอง) ⇒ ปล่อยให้ `set -e` กินตรงนี้ = บั๊กใบนี้
  # กลับมาทางประตูหลัง · `$work` ทั้งก้อนมี `trap` เก็บให้อยู่แล้ว ⇒ พลาดตรงนี้ไม่มีขยะค้าง
  rm -f -- "$tmp" 2>/dev/null || :
  return 0
}

echo "task-move: $found → tasks/$to/$slug"
# ── **ตัวนับคลังของสองลูปข้างล่าง** (ใบ 284 ข้อ 1: "กวาดไปแล้วกี่ไฟล์") — นับ *ไฟล์ที่ถึงมือ
# `edit` จริง* ไม่ใช่ความยาวดิบของคลัง ⇒ ตัวคัดที่พังจะโชว์เป็นเลขเล็กผิดปกติ/ศูนย์ แทนที่จะ
# เป็นรอบที่ **หน้าตาปกติทุกประการแต่ไม่ได้แตะอะไรเลย** (บทเรียน `กวาด 0` ของใบ 193/277)
# ⚠️ **เป็นเลขที่พิมพ์ ไม่ใช่ด่าน** และเส้นแบ่งอยู่ตรงนี้: `md = 0` แปลว่าตัวคัดพัง (ในรีโปจริง
# มีอย่างน้อยตัวใบเอง) แต่ `code = 0` **ถูกกฎ** — ใบที่ไม่มีไฟล์โค้ดไหนอ้างถึงเป็นเรื่องปกติ
# ⇒ ทำให้ศูนย์เป็น FAIL ที่นี่ = แดงใส่รอบที่ถูกต้อง · ตัวที่เป็น *ด่าน* จริงคือ `check-links.sh`
# ที่สคริปต์นี้เรียกท้ายรอบอยู่แล้ว (ชั้นที่หนึ่งของมันมี `กวาด 0 = FAIL` ตั้งแต่ใบ 314) และถ้า
# ตัวคัดของ *ตัวกวาด* พังจนลิงก์ไม่ถูกแก้ เกตตัวนั้นก็แดงให้เองอยู่ดี ⇒ เลขนี้มีไว้ **บอกสาเหตุ**
# ให้คนอ่านรอบที่แดง ไม่ใช่มาเป็นด่านซ้อนด่าน
swept_md=0
swept_code=0
# `-z` + `read -r -d ''` (ใบ 314): pathspec `'*.md'` **ไม่ได้** ช่วยอะไร — มันแมตช์ `.md` ชื่อไทย
# ได้สบาย · ที่ช่วยไว้คือข้อเท็จจริงชั่วคราวว่าวันนี้ยังไม่มี `.md` ชื่อไทยสักใบ
while IFS= read -r -d '' f; do
  [ -e "$f" ] || continue
  case "$f" in .docs/claude-skills-bundle/*|.scratch/*) continue ;; esac
  args=(-e "s#tasks/$from/$esc#tasks/$to/$esc#g" -e "s#\\.\\./$from/$esc#../$to/$esc#g")
  # รูปที่ 3 — เฉพาะไฟล์ที่ **เคย**อยู่ไดเรกทอรีเดียวกับใบนี้ ตอนนี้มันข้ามไดเรกทอรีแล้ว
  case "$f" in "tasks/$from/"*) args+=(-e "s#](${esc})#](../$to/${esc})#g") ;; esac
  # รูปที่ 4 — ไฟล์ที่อยู่ **ใน `tasks/` เอง** (README · HANDOFF-* · PLAN-*) ลิงก์ลงบ้านแบบ
  # `todo/NNN-slug.md` ไม่มี `../` และไม่มี `tasks/` นำหน้า ⇒ ไม่ตรงสักรูปในสามรูปเดิม (task 232)
  # 🔴 **ชั้นที่หนึ่งของเกตเห็นรูปนี้มาตลอด ตัวกวาดไม่เห็น** — ความไม่สมมาตรแบบเดียวกับที่ใบ 168
  # ปิดไปสำหรับไฟล์โค้ด · เจอเพราะ `--human` ของใบ 231 ถูกใช้จริงครั้งแรกแล้วเกตแดงที่
  # tasks/HANDOFF-2026-08-28.md · `tasks/*/*` มาก่อนเพื่อกัน `*` ของ `case` ที่กิน `/` ด้วย
  case "$f" in tasks/*/*) ;; tasks/*) args+=(-e "s#](${from}/${esc})#](${to}/${esc})#g") ;; esac
  swept_md=$((swept_md + 1))
  edit "$f" "${args[@]}"
done < <(git ls-files -c -o --exclude-standard -z '*.md')

# ── ไฟล์ที่ไม่ใช่ `.md`: **ขับด้วย `git grep -l` ด้วยคำสั่งรูปเดียวกับเกตเป๊ะ** (task 168)
# ไม่ใช่รายการนามสกุล — รายการนามสกุลของตัวกวาดที่ไม่เท่ากับขอบเขตของตัวตรวจ คือช่องว่างที่
# ตัวตรวจแดงใส่ของที่ตัวกวาดเอื้อมไม่ถึง ซึ่งคืออาการเดียวกับที่ใบ 062 ตั้งใจปิดเป๊ะ ๆ
# · และไม่ใช่ `git ls-files` ทุกไฟล์ด้วย: `sed` เดินบนไฟล์ binary (lock/รูป/pdf) คือการทำลายของ
# ⇒ `-I` + `-l` ⇒ แตะเฉพาะไฟล์ข้อความที่ **เอ่ยถึงใบนี้จริง ๆ** · ในโค้ดมีแค่รูปที่ 1 (พาธเต็มจากราก)
# `prisma/migrations/**` ตัดออกด้วยเหตุผลเดียวกับเกต: apply แล้วแก้ไม่ได้ (checksum)
# 🔴 **จุดนี้คือ "ตรวจได้แต่แก้ไม่ได้" ของใบ 314** — `git grep -l` เปล่า ๆ quote พาธที่ไม่ใช่ ASCII
# ขณะที่ชั้นที่สามของ `check-links.sh` ทดสอบ `$p` (พาธใบงานที่แมตช์ ซึ่งไม่เคยถูก quote) ⇒ **เกตเห็น
# ไฟล์ชื่อไทย แต่ตัวกวาดข้ามมันทุกครั้ง** ⇒ รัน `task-move.sh` กี่รอบก็ไม่มีวันดับแดงนั้นได้
# = ความไม่สมมาตรที่ใบ 062 ตั้งใจปิด และใบ 232 เจอซ้ำกับรูปลิงก์ที่สี่ (วัดจริง ใบ 314)
while IFS= read -r -d '' f; do
  [ -e "$f" ] || continue
  swept_code=$((swept_code + 1))
  edit "$f" -e "s#tasks/$from/$esc#tasks/$to/$esc#g"
done < <(git grep -z --untracked -I --no-color -l -F "tasks/$from/$slug" \
  -- ':!*.md' ':!.docs/claude-skills-bundle/*' ':!prisma/migrations/*' || true)

# ── ขาออกของไฟล์ที่เพิ่งย้าย: ลิงก์เปล่า ๆ ที่ชี้ไปเพื่อนบ้านซึ่ง **ไม่ได้ย้ายตาม**
moved="tasks/$to/$slug"
while IFS= read -r target; do
  [ -e "tasks/$to/$target" ] && continue
  [ -e "tasks/$from/$target" ] || continue
  edit "$moved" -e "s#](${target//./\\.})#](../$from/${target//./\\.})#g"
# `LC_ALL=C` คู่กับ `-u` เสมอ (ใบ 304) — slug ของใบงานมี `-` เป็นตัวคั่นทุกใบ และใต้
# `en_US.UTF-8` ของเครื่องนี้ วรรคตอน **ไม่มีน้ำหนักในการเรียง** ⇒ `109-a-b.md` กับ `109-ab.md`
# เท่ากันในสายตา `sort -u` ⇒ **ลิงก์ของใบหนึ่งหายไปจากกองที่จะไล่แก้** แล้วเครื่องมือก็เดินต่อ
# จนจบพร้อมพิมพ์ว่าสำเร็จ — ความเงียบแบบเดียวกับที่ใบ 062 เปิดมาปิด (คนที่เจอแดงคือคนที่รัน
# เกตรอบถัดไป ไม่ใช่คนที่ทำให้มันเกิด) · วันนี้ยังไม่มีคู่ไหนชนกันจริง (วัด 2026-09-15: 327
# ลิงก์ดิบ → C และ en_US ได้ 136 เท่ากัน) ⇒ ที่ปิดคือทิศที่เงียบ ไม่ใช่บั๊กที่กำลังทำงานอยู่
done < <(grep -oE '\]\([0-9]{3}-[a-z0-9-]+\.md\)' "$moved" 2>/dev/null | sed -E 's/^\]\(//; s/\)$//' | LC_ALL=C sort -u || true)

# ── ทำให้ผลลัพธ์เป็น **รูปบัญญัติ**: ลิงก์ที่ชี้ไฟล์ในไดเรกทอรีเดียวกันเขียนเปล่า ๆ
# ไม่ใช่ `../<dir>/` ที่ยาวกว่าโดยไม่ได้อะไร · เหตุผลไม่ใช่ความสวย: ถ้าไม่ยุบ **การย้ายไป-กลับ
# จะทิ้ง diff ไว้ทุกครั้งทั้งที่ไม่มีอะไรเปลี่ยนเชิงความหมาย** ⇒ เครื่องมือที่ปั่นไฟล์ทุกครั้ง
# ที่ถูกเรียก คือเครื่องมือที่คนจะเลิกเชื่อ (วัดแล้ว: round-trip ก่อนยุบ ทิ้งความต่างไว้สองไฟล์)
for f in "tasks/$to/"*.md; do
  [ -e "$f" ] || continue
  edit "$f" -e "s#](\.\./$to/${esc})#](${slug})#g"
done
edit "$moved" -e "s#](\.\./$to/\([0-9][0-9][0-9]-[a-z0-9-]*\.md\))#](\1)#g"

# ── บรรทัด `- status:` — **แก้ที่ token ไม่ใช่ทั้งบรรทัด** (task 249)
# เจตนาเดิมถูก: บรรทัดนี้เก็บเหตุผลไว้ด้วย (`**ship แล้ว** (วันที่) — …`) การทับทั้งบรรทัด
# ด้วยคำว่า `done` เฉย ๆ คือการทำข้อมูลหาย ⇒ ของเดิมจึงแตะเฉพาะรูป **เปล่าล้วน**
# 🔴 **แต่รูปเปล่าล้วนเป็นของส่วนน้อย และไม่มีใครวัด**: 98 จาก 242 บรรทัด (40%) มีหาง ⇒ `sed`
# เดิมไม่แมตช์ **แล้วไม่พูดอะไรเลย** ⇒ ใบไปถึงบ้านใหม่พร้อมบรรทัดที่ยังบอกบ้านเก่า · นี่คือกลไก
# ที่ผลิต 11 ใบของ `1e4e3f2`/`dfa355d` ⇒ ทิศที่ผิดคือทิศที่**เงียบ** อีกครั้ง (§6 rule 1)
# ⇒ แก้เฉพาะ token แล้ว**เก็บหางไว้ทั้งดุ้น** — ได้ทั้งสองอย่าง ไม่ต้องเลือก
# · `[^A-Za-z-]|$` ท้ายรูปแบบคือขอบของคำ: `--reopen` ที่ `from=todo` ต้อง **ไม่** แมตช์
#   `todo-human` (ขึ้นต้นเหมือนกันเป๊ะ) ⇒ ขอบเขตของตัวกวาดจึงเท่ากับของตัวตรวจพอดี
edit "$moved" -E -e "s/^(- status: \*{0,2})$from([^A-Za-z-]|\$)/\1$to\2/"

# ── แล้ว**รายงานสิ่งที่เครื่องทำแทนคนไม่ได้** — เกตชั้นที่สี่จะแดงจนกว่าคนจะแก้ (task 249)
# เครื่องมือที่แก้ครึ่งเดียวแล้วเงียบ คือเครื่องมือที่ทิ้งความแดงให้คนที่ย้ายใบ *ถัดไป* ซึ่งเป็น
# อาการเดียวกับที่ใบ 062 เปิดมาปิด · `restore` ของ `counter-test.sh` รายงาน "สิ่งที่ไม่ได้ทำ"
# ด้วยเหตุผลเดียวกันเป๊ะ (§6 rule 8)
st="$(grep -m1 -E '^- status:' "$moved" || true)"
tok="$(printf '%s' "$st" | sed -E 's/^- status: *//; s/^\*\*//' | grep -oE '^[a-z][a-z-]*' || true)"
case "$tok" in
  todo|todo-human|done)
    [ "$tok" = "$to" ] || {
      echo "task-move: ⚠️ บรรทัด status ยังเขียน \`$tok\` (ไม่ใช่ $to) — แก้เอง:"
      echo "           $st"
    } ;;
esac
case "$st" in
  "- status: $to") ;;                       # เปล่าล้วน ไม่มีหางให้ทาน
  *) [ -z "$st" ] || echo "task-move: ⚠️ หางของบรรทัด status ถูกเก็บไว้ทั้งดุ้น — อ่านว่ายังจริงอยู่ไหม" ;;
esac
if [ "$to" != "todo-human" ] && grep -q '^- 🚫' "$moved"; then
  echo "task-move: ⚠️ ใบนี้ยังมีบรรทัดขึ้นต้น \`- 🚫\` (ประกาศว่าบล็อกที่คน) แต่บ้านใหม่คือ tasks/$to/"
  echo "           ⇒ ลดชั้นเป็น \`- (เดิม) บล็อกที่: …\` แบบที่ใบ 166 ทำ (เก็บข้อความไว้ ไม่ใช่ลบทิ้ง)"
  echo "           เกตชั้นที่สี่จะแดงจนกว่าจะแก้ — ตั้งใจให้แดง ไม่ใช่บั๊ก"
fi

# ⚠️ **`|| :` ปิดท้าย** (รอบรีวิวของใบ 284) — นี่คือ `||` list ที่คำสั่ง**ตัวท้าย**ยังถูก `set -e`
# กินอยู่ ⇒ `rm -rf` ที่ล้ม (ไดเรกทอรีอ่านไม่ได้ · ถูกลบไปแล้วโดยอย่างอื่น) จะฆ่ารอบ **ก่อนบล็อก
# รายงาน staged/left/🔴/⛔ และก่อนเกต** = กลืนรายงานความล้มเหลวที่ใบนี้ทั้งใบมีไว้พิมพ์ ซึ่งเป็น
# บั๊กเดิมกลับมาทางประตูหลัง (เหตุผลเดียวกับ `rm -f -- "$tmp" … || :` ใน `edit()`) · `trap`
# เก็บให้อยู่แล้ว ⇒ บรรทัดนี้ล้มแล้วไม่มีขยะค้าง มีแต่เกตที่จะเห็นไฟล์ใน `$work` เพิ่มมา
rmdir "$work" 2>/dev/null || rm -rf "$work" || :

# ── **คลังที่กวาดไปจริงกี่ใบ** (ใบ 284 ข้อ 1) — พิมพ์ก่อนบล็อกรายงานทุกกอง เพราะมันคือ*ตัวหาร*
# ของทุกเลขที่ตามมา: "แก้ 2 ใบ" อ่านได้คนละเรื่องเลยระหว่างคลัง 1,400 ใบกับคลัง 3 ใบ
echo "task-move: กวาด .md $swept_md ใบ · ไฟล์โค้ด $swept_code ใบ"

# ── ทานกับสิ่งที่ git เห็นจริง (task 276, รอบรีวิว) — **ห้ามเชื่อบัญชีของตัวเองอย่างเดียว**
# `edit()` จดชื่อได้เฉพาะไฟล์ที่มันรู้จัก ⇒ ทางที่มันเขียนของโดยไม่รู้ตัว (เขียนทะลุ symlink เป็นต้น)
# จะไม่อยู่ในกองไหนเลย = เปลี่ยนแล้ว ไม่ถูก stage และ **ไม่มีใครเอ่ยชื่อ** ซึ่งคือทิศเงียบที่ §6 rule 1
# ห้ามไว้ · ด่านนี้ไม่ต้องรู้ว่ามันเกิดทางไหน แค่ถาม git ว่า "ยังมีอะไรค้างที่ไม่มีใครอธิบายไหม"
unaccounted=()
# `failed` อยู่ในบัญชีด้วย (ใบ 284) — ไฟล์ที่ `cat` ล้ม *หลัง* `>` ตัดไฟล์ทิ้งแล้วจะโผล่ใน
# `git diff` ⇒ ถ้าไม่นับมันเป็น "รู้จัก" กอง 🔴 จะเรียกมันด้วยสาเหตุที่ผิด ("ไม่รู้ว่าตัวเองแก้")
# ทั้งที่กอง ⛔ ข้างล่างเรียกชื่อมันไว้แล้วพร้อมสาเหตุจริง — สองข้อความสำหรับสาเหตุเดียวคือความสับสน
# 🔑 **ทิศนี้ถูกวัดแล้ว ไม่ใช่คำกล่าวอ้าง** (รอบรีวิวของใบ 284 ทักว่ามันเป็นการแก้ที่ไม่มีอะไรเฝ้า):
# เคส **AE** วาง shim ของ `cat` ไว้ใน PATH ที่เขียนไฟล์ไม่ครบแล้วคืน 1 ⇒ ไฟล์ถูกตัดจริง โผล่ใน
# `git diff` จริง · และ **ความไว 19** ถอดชื่อ `failed` ออกจากบรรทัดนี้แล้ววัดว่าไฟล์เดียวกันถูก
# เรียกชื่อซ้ำในกอง 🔴 ด้วยสาเหตุที่ผิด — ฉากที่ AB/AD ให้ไม่ได้ เพราะที่นั่น `>` เปิดไม่ผ่านตั้งแต่ต้น
known="$(printf '%s\n' ${staged[@]+"${staged[@]}"} ${left[@]+"${left[@]}"} ${failed[@]+"${failed[@]}"} ${dirty_before[@]+"${dirty_before[@]}"})"
while IFS= read -r -d '' f; do
  [ -n "$f" ] || continue
  grep -qxF -- "$f" <<<"$known" && continue
  left+=("$f"); unaccounted+=("$f")
done < <(git diff -z --name-only)

# ── รายงานการ stage (task 276) — **ทั้งสองกอง ไม่ใช่แค่กองที่มีปัญหา**
# กองที่ stage ให้แล้วต้องพิมพ์ด้วย เพราะเครื่องมือที่แตะ index ของคนอื่นแบบเงียบ ๆ คือเครื่องมือ
# ที่คนจะเลิกเชื่อ · และ `git mv` เองก็ stage การย้ายไปแล้วตั้งแต่ต้นสคริปต์ ⇒ พูดให้ครบทีเดียว
# `edit()` ถูกเรียกซ้ำได้หลายรอบต่อไฟล์เดียว (ใบที่ย้ายโดนถึงสี่รอบ) ⇒ **นับจากรายชื่อที่ยุบซ้ำแล้ว**
# ไม่ใช่จากความยาวดิบของอาเรย์ ไม่งั้นเลขกับรายชื่อที่พิมพ์ใต้มันไม่ตรงกัน
# `LC_ALL=C` คู่กับ `-u` เสมอ (ใบ 304) — พาธมีทั้ง `/` `-` `_` และใต้ `en_US.UTF-8` ของเครื่องนี้
# วรรคตอนมี **น้ำหนักการเรียงเป็นศูนย์** ⇒ `a/b.txt` กับ `a-b.txt` เท่ากันในสายตา `sort -u`
# ⇒ กองนี้คือกองที่เครื่องมือ **เรียกชื่อสิ่งที่มันแก้ไม่ได้ออกมา** (§6 rule 1) ⇒ ยุบหายหนึ่งชื่อ
# = ไฟล์ที่ไม่มีใครเอ่ยถึงเลยสักบรรทัด ซึ่งคือความเงียบที่กติกาข้อนั้นเปิดมาปิดพอดี · และเลขที่
# พิมพ์ต่อท้ายก็นับจากกองที่ยุบแล้ว ⇒ **ตัวเลขกับรายชื่อจะพร้อมใจกันผิดไปทางเดียวกัน** ไม่มีใคร
# ขัดกันเองให้เห็น · วันนี้ยังไม่มีคู่ไหนชนกันจริง (วัด 2026-09-15: 2,458 พาธ → C และ en_US ได้
# 2,458 เท่ากัน) ⇒ ที่ปิดคือทิศที่เงียบ ไม่ใช่บั๊กที่กำลังทำงานอยู่
uniq_list() { printf '%s\n' "$@" | LC_ALL=C sort -u; }   # เรียกเมื่อกองไม่ว่างเท่านั้น
if [ "${#staged[@]}" -gt 0 ]; then
  staged_u="$(uniq_list "${staged[@]}")"
  echo "task-move: git add ให้แล้ว $(wc -l <<<"$staged_u" | tr -d ' ') ไฟล์ (ไม่มีงานค้างที่ยังไม่ stage อยู่ก่อน ⇒ ที่เพิ่มเข้า index คือสิ่งที่เครื่องมือเขียนเท่านั้น)"
  sed 's/^/           + /' <<<"$staged_u"
fi
if [ "${#left[@]}" -gt 0 ]; then
  left_u="$(uniq_list "${left[@]}")"
  echo "task-move: ⚠️ อีก $(wc -l <<<"$left_u" | tr -d ' ') ไฟล์ **ไม่ได้ stage ให้** — มีงานค้างของคุณอยู่ก่อน · ยังไม่ถูก track · เป็น symlink · \`git add\` ล้ม · หรือเครื่องมือไม่รู้ว่าตัวเองแก้ (ดู 🔴 ข้างล่าง)"
  # ⛔ **เคยเติมบรรทัด `→ ของจริงคือ <readlink>` ให้ entry ที่เป็น symlink แล้วถอดออก** (ใบ 276
  # รอบรีวิวที่สอง) — มันสะดวกจริง แต่มันพิมพ์ชื่อไฟล์ปลายทางออกมา **ในทุกกรณีที่ symlink เข้ากอง
  # นี้** ⇒ ตัวทานท้ายรอบ (กอง 🔴) ไม่เหลือฉากไหนที่มันเป็นคนเดียวที่จับได้ ⇒ **ความไว 12 เขียว
  # ทั้งที่ถอดตัวทานออกแล้ว** (วัดจริง) · ยามที่วัดไม่ได้คือยามที่ไม่มีอยู่ ⇒ เลือกเก็บ*ตัวทาน*
  # ซึ่งปิดได้ทั้งคลาส ไม่ใช่เก็บ*ความสะดวก*ที่ปิดได้เคสเดียว — กอง 🔴 บอกชื่อปลายทางอยู่แล้ว
  # เว้นกรณีเดียวคือปลายทางเป็นงานค้างของคนเอง ซึ่งอยู่ใน `git status` ของเขาอยู่แล้ว
  sed 's/^/           ? /' <<<"$left_u"
  echo "           ⇒ stage ให้เองแปลว่าลากงานที่คุณยังไม่ตั้งใจ commit เข้าไปด้วย (§5/§6 rule 4 บังคับให้บางคอมมิตแยก stage)"
  echo "           ⇒ อ่านแล้ว \`git add\` เอง — ถ้าลืม \`check-links.sh\` จะ **เขียวบนเครื่องคุณ แต่แดงที่ HEAD**"
fi
if [ "${#unaccounted[@]}" -gt 0 ]; then
  echo "task-move: 🔴 ในนั้นมี $(uniq_list "${unaccounted[@]}" | wc -l | tr -d ' ') ไฟล์ที่ **เครื่องมือไม่รู้ว่าตัวเองแก้** — git เห็น แต่บัญชีของเครื่องมือไม่มีชื่อ"
  uniq_list "${unaccounted[@]}" | sed 's/^/           ! /'
  echo "           (ทางที่พบแล้ว: เขียนทะลุ symlink ⇒ ของจริงเปลี่ยนแต่ชื่อที่กวาดคือตัวลิงก์) — ดูให้ดีก่อน \`git add\`"
fi

# ── กอง ⛔ (ใบ 284): **สิ่งที่มันกวาดไม่สำเร็จ** — พิมพ์ท้ายสุดก่อนเกต เพราะเป็นกองที่อธิบาย
# ความแดงที่กำลังจะตามมาสองบรรทัดถัดไป · สัญญาเดียวกับหาง `- status:` และบรรทัด `- 🚫`:
# สิ่งที่เครื่องตัดสินได้ มันทำ · สิ่งที่ตัดสินแทนคนไม่ได้ มันตั้งชื่อแล้วปล่อยให้คนแก้
if [ "${#failed[@]}" -gt 0 ]; then
  echo "task-move: ⛔ ${#failed[@]} ไฟล์ **กวาดไม่สำเร็จ** — ที่เหลือกวาดจบครบทั้งชุดแล้ว (ไม่ได้หยุดคาที่)"
  i=0
  while [ "$i" -lt "${#failed[@]}" ]; do
    echo "           ⛔ ${failed[$i]} — ${failed_why[$i]}"
    i=$((i + 1))
  done
  # ทางออกที่ **พิมพ์ตามได้จริง** — ไป–กลับหนึ่งรอบกวาดทั้งสี่รูปให้ใหม่หมด และเคส M ของ selftest
  # วัดไว้แล้วว่ารอบไป–กลับไม่ทิ้ง diff (เทียบด้วย `git write-tree`) ⇒ ไม่ใช่คำแนะนำลอย ๆ
  echo "           ⇒ ใบถูก \`git mv\` ไปแล้ว แต่ลิงก์ในไฟล์ข้างบนยังชี้ tasks/$from/ ⇒ เกตจะแดงต่อ (ตั้งใจ)"
  echo "           ⇒ แก้เหตุก่อน (บ่อยสุด: \`chmod u+w <ไฟล์>\`) แล้วกวาดใหม่ทั้งชุด:"
  echo "              bash scripts/task-move.sh $num$rev_flag   แล้ว   bash scripts/task-move.sh $num$fwd_flag"
fi

# เครื่องมือที่ไม่ตรวจผลของตัวเอง คือเครื่องมือที่เชื่อไม่ได้อีกอัน (ข้อสุดท้ายของใบ 062)
# ⚠️ **อ่าน rc ของเกตแทนที่จะให้ `set -e` จบให้** (ใบ 284) — กอง ⛔ ต้องทำให้ exit ไม่เป็นศูนย์
# **ถึงแม้เกตจะเขียว**: ไฟล์ที่กวาดไม่สำเร็จอาจเป็นไฟล์ที่ **ไม่มีชั้นไหนของ `check-links.sh` เฝ้า**
# (เช่นพาธใบงานใน `.md` ที่เขียนในแบ็กทิก ไม่ใช่รูปลิงก์ markdown — ข้อที่ชั้นที่หนึ่ง*ยังไม่*ปิด)
# ⇒ ปล่อยให้จบ 0 เมื่อไร เครื่องมือก็รายงานความล้มเหลวด้วยหน้าตาของความสำเร็จ ซึ่งคือคลาสเดียวกับ
# `--humen` ของใบ 231 · ⚠️ **และห้ามอ่านประโยคข้างบนว่า "ทั้งรีโปไม่มีใครเฝ้าพาธในแบ็กทิก"** —
# `check-card-paths.sh` เฝ้าคลาสนั้นอยู่ (มันยกเว้นไฟล์ที่ *อยู่ใน* `tasks/**` ไม่ใช่พาธที่ *ชี้ไป* หา)
# แต่สคริปต์นี้ไม่ได้เรียกมัน ⇒ เหตุผลที่ถูกคือ **rc ของเครื่องมือต้องไม่อ้างความสำเร็จแทนเกตที่มัน
# ไม่ได้รัน** ไม่ใช่ "ไม่มีใครเฝ้า" (รอบรีวิวของใบ 284 ทักคำนี้ไว้)
gate_rc=0
bash scripts/check-links.sh || gate_rc=$?
if [ "${#failed[@]}" -gt 0 ] && [ "$gate_rc" -eq 0 ]; then gate_rc=1; fi
exit "$gate_rc"
