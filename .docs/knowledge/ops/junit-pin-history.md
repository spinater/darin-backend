---
sources:
  # Every row of this card is one row of that file, and vice versa. It is the **only** source: this
  # card holds the *history* of the pin layer — what each raise or split bought — while the layer's
  # rules, the split rule and the "no test here reaches a database" conclusion stay in
  # [gate-tiers-and-pins.md](gate-tiers-and-pins.md). Split at ใบ 068 because the history is the half
  # that grows: five review rounds of ใบ 065 alone took the parent 183 → 194 against a 200 cap, and
  # every one of those lines was a table row, not a rule.
  - scripts/junit-pins.txt
---

# ประวัติหมุด junit — หมุดแต่ละตัวขึ้นมาเพราะอะไร และมันซื้ออะไรมา

`scripts/junit-pins.txt` is the authority for the **numbers**; this card is the authority for
**why**. A raise with no entry here is a raise nobody can audit later, and §7 treats a *lowering* as
a declaration that coverage was removed — so the reason for one belongs in its task card **and**
here.

Read [gate-tiers-and-pins.md](gate-tiers-and-pins.md) first for what a pin is and cannot do: it
counts `tests - skipped`, so it cannot see an assertion getting weaker, and no test in this repo can
reach a database.

## How the numbers got here, card by card

Task 009 raised `lib/payroll.test.ts` 21 → **24** and added
`lib/ot-import.test.ts` at **6** and `lib/next-errors.test.ts` at **6**; task 013 raised
`lib/payroll.test.ts` to **26** and added `lib/payroll-run.test.ts` at **4** (5 → 4 inside that
same round: two scope tests merged into one whole-object comparison pinning strictly more — a
merge, not a removal), and item 4 of the same card added `lib/form-number.test.ts` at **11** and
`lib/config-form.test.ts` at **7** and raised `lib/ot-import.test.ts` 6 → **7**; its review round
then raised all three again — `lib/form-number.test.ts` → **13**, `lib/config-form.test.ts` → **12**
and `lib/payroll.test.ts` 26 → **30**; task 025 then took `lib/payroll.test.ts` 30 → **33**.
**ใบ 058** added `lib/gymmo.test.ts` at **16** — the reader for the Gymmo trainer-worklog export.
Sixteen is what it takes to pin the two things that fail *quietly* in a worklog reader: a date that
parses into the **wrong month** (`31 SEP` rolls to 1 OCT under `Date.UTC`, moving a session across
the boundary payroll buckets by), and a row that is **dropped instead of reported** — a session
nobody gets paid for, the §2 rule 4 silent zero. Three of the sixteen are a `test.each` over the
three unreadable columns (date · type · counts) asserting the row is *reported*, not skipped; one
pins `Attendees` staying verbatim, because a real row reads `เอรา,อลัน เอรา,อลัน` and splitting on
the comma invents people (task 049 §8.3); one pins that `attendedOf` does **not** clamp a negative,
because clamping hides the row from the task 025 guard in `lib/payroll.ts` that warns on it.

และ `lib/gymmo-map.test.ts` at **15** — the naming seam between that export and this database.
The arm that earns its keep is **"an exact name wins over an alias"**: it is what lets a real
`ClassPrice` row named as Gymmo spells it retire an alias *by existing*, so the table cannot rot
into a graveyard nobody dares delete from. Two more pin the direction of failure — a class with no
price and an alias pointing at a price that does not exist are both **refused with a reason**, never
guessed, because a guessed class name pays the wrong ราคา silently. One pins that the alias targets
are distinct, so no two Gymmo names can quietly collapse onto one priced class. And three pin
`(Deleted)`: Gymmo marks a trainer who has left by appending it to the sheet name, and their past
คาบ are still owed — dropping that sheet is the §2 rule 4 silent zero wearing a different hat.

**ใบ 064** moved three rows at once — `lib/gymmo.test.ts` **16 → 19**, `lib/gymmo-import.test.ts`
**20 → 22**, and a new `lib/class-problems.test.ts` at **9** (its fix round added the last of each:
16→18→19 and 6→9). 🔑 **No pin was lowered**, and the
reason that is worth saying is that the card *re-shaped* assertions wholesale: `GymmoParse.problems`
became `GymmoReadProblem[]`, so every `expect(problems[0]).toContain(...)` became a field read. The pin
counts `tests - skipped`, so re-shaping cannot move it — only added arms can, which is exactly the
property that lets a refactor of this size be reviewed on its diff instead of on its numbers.
The arm to protect in the new file is **"a session key is byte-identical to `gymmoSourceKey`"**:
clearing a problem is a keyed `deleteMany` over the `sourceKey`s the import writes, so one byte of
drift leaves a fixed คาบ queued for ever and `/payslips` blocking a clean period — the card shipping
and doing nothing. Counter-tested on a clean tree (`restore`'s sha256 compare passing, `git status`
byte-identical afterwards): re-deriving that key instead of calling `gymmoSourceKey` turns **8** arms
red across the two files, named in the pin row. ⚠️ The transaction that does the clearing is **reviewed, not pinned**, for the
structural reason below (no test here reaches a database), which is also why
`applyGymmoImport`'s `!writes.length && !problems.length` guard has to be read by eye.

**ใบ 065** raised `lib/payroll/class.test.ts` **6 → 7** and added `lib/gymmo-hand-keyed.test.ts` at
**5**, then `lib/class-problems-copy.test.ts` at **7** and `lib/gymmo-import.test.ts` **22 → 24**
across its five review rounds (22 test files → 24).
🔑 **That last file is the lesson of the round**: the same defect BLOCKed twice — an exit instruction
keyed on too little state, telling the admin to re-import a คาบ already in the database — and both
times it was reachable only by reading, because the strings lived in a `.tsx`. Moving them to a pure
`(kind, state)` grid is what made a pin possible at all; one arm walks every cell. The arm worth naming is the `|`-collision one: `2026-08-04|"s-o|c-core"|"x"`
and `2026-08-04|"s-o"|"c-core|x"` are different triples with one joined key, so a joined key reports a
duplicate คาบ that does not exist and a human deletes a real row — the ใบ 035 bug class on a new key.
Counter-tested, all three breaks named in the pin rows.


## What each raise bought

| Pin | Raise | What it is worth |
| --- | --- | --- |
| `lib/payroll.test.ts` | 21 → 23 | two §2-rule-4 cases — a membership sale attributed to a non-`closer`, and an unknown `sale.kind`; each asserts a warning **and** a zero commission |
| `lib/payroll.test.ts` | 23 → 24 | the OT >2-decimal case: 9:20 arrives as `9.333333333333334`, the engine pays 13.33 and 266.67 over 20 days (§2 rule 5, round once) — now `lib/payroll/ot.test.ts` (ใบ 037), whose pin comment names the card quoting those figures: `money-on-screen.md` since ใบ 042, `payroll-rules.md` before it |
| `lib/ot-import.test.ts` | new at 5 | usernames from a fingerprint paste that match nobody reach the screen instead of the floor |
| `lib/ot-import.test.ts` | 5 → 6 | `invalidHours` — a non-finite hours value is rejected by the parser, because `OtEntry.hours` is a `Float` ⇒ `double precision`, which **accepts `NaN`** |
| `lib/next-errors.test.ts` | new at 6 | telling a thrown `redirect()` apart from a real error, so the `/ot` action's `catch` cannot swallow navigation |
| `lib/payroll.test.ts` | 24 → 26 | task 013 item 3 — an inactive staff member's slip **warns and changes no figure**; the second test compares the whole result against the same input with `active: true`, so a later "pay them 0" is red |
| `lib/payroll-run.test.ts` | new at 4 | task 013 items 1 and 3 as *predicates*: the non-draft guard's wording and refuse-by-default shape, the two refusals staying distinguishable **in the returned `skipped` list** (no screen renders them — the `compute` action discards `runPayroll`'s return, and what the admin sees is the state-derived `closedCount` box and the `— (ไม่ได้คำนวณใหม่)` marker), and the six-arm staff scope including the leaver arms. **Not** the lock itself — the `count === 0` branch needs two concurrent sessions — and **not** that the leaver arms return a row, which needs a real Postgres; see the row below |
| `lib/form-number.test.ts` | new at 11 | the one predicate every form field that becomes money goes through — absent, blank, a `File` part, `"1e999"` and a negative value each refused, plus `isBlank` keeping a `File` **out** of the "not given" bucket instead of letting it take the field's default |
| `lib/config-form.test.ts` | new at 7 | `/admin/config`'s bulk save parsed **all-or-nothing** before its first write: a blank rate still deletes that rate, a blank salary refuses, and one bad field refuses the whole save — which is exactly what the screen promises the admin ("ยังไม่ได้บันทึกอะไรเลยสักช่อง") |
| `lib/ot-import.test.ts` | 6 → 7 | the **negative** hours value the `isFinite` check let through — it is finite, so it stored and then paid nothing through `Math.max(0, -5 − threshold)`. Pinned because the form had just started refusing it and the paste is the path OT actually arrives by |
| `lib/form-number.test.ts` | 11 → 13 | `int` and `max`. Measured against this schema: Postgres `integer` **truncates** `2.5` to `2` rather than refusing it (`booked`, `baseSalary`, `ClassPrice.price` all), and an out-of-range value throws **at the write**, mid-loop. Both are now refused before the first write |
| `lib/config-form.test.ts` | 7 → 12 | `cfg\|…` joining the parse — the largest hole of the five, since those boxes are plain text with no `required` and a cleared one stored `""`; plus the `Int` ceiling, the fraction that used to be pinned as *valid*, and a `staff\|…` field the form does not render (which used to throw mid-loop) |
| `lib/payroll.test.ts` | 26 → 30 | `num()` throwing on a blank or non-finite config value, pinned at the engine rather than at the form — including the 20,000 ฿ self-closed bill that paid **0 ฿** instead of 2,000 ฿ with `warnings: []` when `comm.pt.selfClosed` was cleared |
| `lib/payroll.test.ts` | 30 → 33 | task 025 — a **negative** attendance (`noShow > booked`, which stored before `/classes` refused the pair) warns with the class and the two counts **in order** instead of folding into the engine's `<= 0` arm for 0 ฿ with `warnings: []`; the opposite boundary, an attendance of exactly 0, which must **not** warn; and one warning **per row**, since `byClass` merges the payslip lines by class name and a "dedupe the warnings" tidy-up would otherwise stay green. 🔑 The money half asserts the class **line**, not `classPay` — `Math.max(0, … − classCredit)` clamps the latter, so a row that *subtracted* 400 ฿ would still read 0 |
| `lib/payroll.test.ts` | 33 → 34 | task 034 — an activity named `__proto__`, configured at a **real** rate, must not be inherited by any other activity: an unconfigured one still warns and stays unpaid. 🔑 Written against that worst case on purpose — a lookup that merely returned `undefined` for the missing activity would pass while the *write* was still polluting the process, which is why the source-level half is pinned next door |
| `lib/payroll-run.test.ts` | 4 → 6 | task 034 — `buildTeachRates` was extracted out of `runPayroll` (into `lib/payroll.ts`, beside the type it builds, so the engine's own money tests never import `lib/db.ts`) so the rate map could be asserted without a database. One test pins that the fold never reaches `Object.prototype` (`Object.hasOwn(Object.prototype, "ST")`, and a fresh `{}` too — that is how the damage was felt), one that several ranks of one activity merge rather than overwrite |
| `lib/staff-form.test.ts` | new at 9 | ใบ 027 — nine arms over the **four** refusals `parseNewStaff` can return: the three identity fields, which `addStaff` used to answer with a bare `return` (revalidate, clear the form, say nothing), plus task 013's money flag. They pin the **order** — name → username → password → the money fields — so two bad fields always report the same one; the password boundary at `MIN_PASSWORD_LEN` with the password left **untrimmed** (a padded one must hash exactly as posted, or the staff member cannot log in with what they were handed); and task 013's money arm unmoved — blank ⇒ 0, unreadable ⇒ refuse, asserted on **both** fields, since they are read by two separate lines and pinning one leaves a tidy-up of the other green. 🔑 The fifth flag, the duplicate username, is **not** pinned: `P2002` comes from the unique index, so it needs a real Postgres, which no `bun test` here has |
| `lib/activities.test.ts` | new at 5 | task 036 — the activity-name union, pinned where it is pure (`mergeActivityNames`). What it buys is that **an activity can exist without a rate**: `addActivity` used to create three `TeachRate` rows at `rate: 0`, so `rate == null` in the engine was unreachable for a screen-added activity and a 12-session ST trainer was paid **0 ฿ with `warnings: []`**. The load-bearing arm is the *empty registry* one — a name with a rate row, a sheet source or one historical session must still render, so no rate can vanish from the matrix because a registry row is missing. 🔑 The money boundary itself (a rate of `0` vs a missing rate) is **not** here: it is the engine's, and task 037 owns it in the engine suite — this file must not grow a second copy |
| `lib/seed-policy.test.ts` | new at 8 | task 040 — what `prisma/seed.ts` may assert about the database in front of it. The load-bearing arm is `seedMode({marked: false, staffCount: 7}) === "adopt"`: reading "no seed mark" as "fresh database" would plant the reference fixture over the deploy host, i.e. ship the card's own bug once, on the only database with real money in it. Seven more: the empty-database arm, the mark beating the witness in **both** directions, a whole-object compare of `plantsFixture`/`recordsMark` across all three modes, and four on the report builder — including 🔴 the **omission**, that no mode may list a missing `TeachRate` cell. Pinned by whole-array comparison, because an added line is green against every membership assertion. ⚠️ What it does **not** prove is that the seed withheld anything; that is the row below |
| `lib/gymmo-import.test.ts` | new at 20 | task 063 — the write plan that turns a Gymmo export into `ClassSession` rows. The load-bearing arm is the **`sourceKey` collision pair**: a sheet name and a class name are both free text, so a `sheet|date|time|class` join merges two genuinely different คาบ into one key, and the consequence is not a wrong screen but a คาบ that vanishes or a payment that doubles (the ใบ 035 bug class, measured against the real join in the test itself). Beside it: the key is asserted by **`JSON.parse` round-trip**, not by its spelling, because decodability is what injectivity actually rests on; two rows of one file with the same key send **both** to `problems` and write neither; a negative attendance is written **verbatim**, which is what keeps task 025's engine warning reachable; a `(Deleted)` suffix — which Gymmo **appends to the sheet name of a trainer who leaves** — must not produce a new key, or a leaver's nine months of คาบ re-import as `create` and are paid twice; and the last arm carries 24 synthetic August คาบ through `computePayslip` to **3,050 ฿**. ⚠️ Synthetic, and the class **mix** in it is invented, so it cannot fail for the reason the real run is most likely to: an unpriced or un-aliased class in ธันยา's actual August would land in `problems`, the real class value would come in under 8,050, and every assertion here would still be green ⇒ this pin proves the import→engine seam, **not** ใบ 063 §4 |
| `lib/payroll/slip.test.ts` | 3 → 5 | task 063 — the engine had **no test at all** for `baseSalary: 0`, which this change newly depends on (the seed now plants two trainers at 0 because nobody has said what they are paid). A 0 base emits no line, so the slip was silent about a number nobody had configured: `net 0.00`, `warnings: []`. The second arm pins the **predicate** (`role === "trainer"`), which a `!base`-only version passes while warning on every owner slip |
| — (no pin: not a `bun test`) | stage 5 runs the seed **twice**, around two broken rows | task 040's layer 2, and the only place "plants once" is asserted against a real database. Between the two runs `db_stage` **deletes** the `pt × PT` `TeachRate` and **sets** `comm.pt.selfClosed` to `8` with `psql` in the throwaway container; after the second run it requires `seed: mode=already-initialized created=0` in the log **and reads both rows back** — the rate still absent, the value still `8`. The grep alone is not enough and measuring that was the point: `created=<n>` is a counter a developer increments by hand at seven `writes++` sites, so an `update`-shaped regression (`data: { note }` → `data: { value, note }`, the most tempting tidy-up in the file) and a re-creating write that forgets its `writes++` both report `created=0` honestly — **both were run as mutants and both pass the grep**; the two `psql` reads are what kill them. Every step fails **closed**: a `psql` that cannot reach the container, a query that errors, an empty result, and a missing log are each a red, all four proven by counter-test — and since the round-2 review a fifth: the value is **read before** the `UPDATE` and must not already be `8`, because the read-back only discriminates while `CONFIG_DEFAULTS["comm.pt.selfClosed"]` is something else. Edit that default to `8` in a later card without the guard and the assertion writes `8`, reads `8`, and passes green over the very regression it was added for. It is **not** a junit report and deliberately not one — a second report inside `db_stage` slips past the layer-2 corpus filter (the three-attempt table in [gate-tiers-and-pins.md](gate-tiers-and-pins.md)) |
| `lib/payroll.test.ts` → six rows under `lib/payroll/` | 34 → **6 · 6 · 14 · 3 · 3 · 4** | task 037 — the first **split** in this table, and so the first entry that is not a raise. The file had reached 468 lines against §4's 500 (`git show HEAD:lib/payroll.test.ts | wc -l`; the 037 card's title says 469), and the test the card adds was the one that had to pay for it. The six files are `teach` (§1.2) · `class` (§1.4) · `commission` (§1.5 · §1.6 · §2.2) · `ot` (§2.4) · `slip` (§1.7 and the inactive-staff pair) · `config-guards` (`num()`), with the shared `computePayslip` input builder in `lib/payroll/fixtures.ts`. **+2** of the 36 are new: the `rate == null` boundary — a rate configured at exactly `0` pays 0 with `warnings: []` and still emits its teach line, a *missing* rate warns and emits none |
| `lib/color-rules.test.ts` | new at 12 → **14** | ใบ 043 — the fold that names สีที่กำลังจ่ายโดยไม่มีใครรับรอง. The load-bearing arm is **"answered but not applied"**, and it is there because `payroll-auditor` BLOCKed the first version for not having it: `ColorRule` is applied **at sync time only** and `syncSources()` skips a `reviewed: true` row *before* the colour lookup, so writing the rule does not move the คาบ already in the database. A fold that retired a colour the moment it was answered went quiet at exactly the moment the money was most wrong — 12 ว่ายน้ำ คาบ × 250 = **3,000 ฿** paid for คาบ the owner had just declared cancelled, the only screen that said so switched off by their own correct answer. Its twin arm is that a swept colour really does leave the list, or the queue could never reach zero. **+2 in round 2, also `payroll-auditor`**: `review` needed its own count — a hand-cleared row is not in *open* conflict with a `review` rule, so `pending = sessions − reviewedSessions` for `review` only, while under `skip` it still disagrees (a human said pay, the colour says do not, and which wins is ใบ 066/069's question) and stays counted. 🔴 **Round 3 inverted one of those two arms and it is now the most valuable in the file**: `payroll-auditor` audited its own round-2 request and found that `reviewed: true` is **not evidence about a colour** — `/sync/review` never renders `bgColor`, and the one note that carried it (`สีในชีตต้องให้คนตรวจ (#hex)`) is overwritten with `"คนตรวจยืนยันแล้ว"` on resolve, with no `reviewedAt` to date it against the rule. So `pending === 0` may **not** retire a colour: on ว่ายน้ำ, where every payable คาบ is hand-cleared by construction, the honest answer "ให้คนตรวจ" silenced all three screens in one page load over 40 × 250 = 10,000 ฿ nobody had looked at. The arm that asserted `[]` now asserts the colour **emitted at `pending: 0`**, and the count stayed 14 because it was a replacement, not an addition. Beside them: `pay` is the only answer that retires a colour alone (the only one that *agrees* with the rows) · a `meaning` outside the closed set counts as **unruled**, because that is what `lib/sync.ts` does with it · `reviewedSessions` survives the fold because it is the count of คาบ **no sync will ever repair**, and ใบ 070 turned it into the link to the one screen that can (`/sync/review?hex=`) · **14→16 ใบ 070**: `isReportableHex` + `payableWithColorWhere`, the guard between a URL and a bulk ข้าม — `?hex=%23ffffff` would list the ~320 payable คาบ a month that carry white (no swatch counts white) at 50 × 250 = 12,500 ฿ a page, and `%`/`_` are **ILIKE wildcards** rather than letters, so `?hex=%25` matches every coloured payable คาบ of every period. `payableWithColorWhere` returns `null` rather than a wider filter so the caller cannot forget the guard. ⚠️ The *query* still reaches no database here; what is pinned is the decision "may this string be aimed at rows at all", which is where the money risk is · case folded on both sides · `#ffffff`/`null` never reported (an unstyled cell and a white fill are the same bytes). Counter-tested 2026-09-21, one mutant at a time per ใบ 067: `if (meaning === "pay")` → `if (meaning)` (round 1's exact defect) **3 fail** · dropping the `review` asymmetry **2 fail** · `pending > 0 || reviewedSessions > 0` → `pending > 0` (round 3's exact defect) **1 fail**. ⚠️ A fourth mutant that killed an arm on the round-3 fold, `pending > 0` → `>= 0`, is **subsumed** on the shipped one and was retired from the list rather than carried forward — a counter-test number nobody can reproduce reads as proof an arm is load-bearing when it is not (ใบ 067's own lesson). ⚠️ What it does **not** prove is the query: that the filter mirrors `runPayroll`'s (`status: "ok"` · period · `staffId` not null) reaches a database and no test here does |
| `lib/ot-import.test.ts` | 7 → 12 | task 014 — the paste write became **one `$transaction`**, and a transaction is a *regression* unless every per-row data problem is refused before it: one bad line used to strand 84 of 220 rows committed, and now it would roll back all 220. Five arms pay for that, three of them over cases that were **silent**. 🔴 The empty hours cell (`somchai<TAB>2026-07-01<TAB>`) reached **no bucket at all** — not `rows`, not `unmatched`, not `invalidHours` — because the old skip (`!user \|\| !date \|\| !hours`) dropped it before the username lookup, so a fingerprint export's missed scan-out reported "นำเข้าแล้ว 219 รายการ" with zero warnings while that person's OT was short. 🔴 The **rolled-over date** is worse than the unparseable one: `new Date("2026-06-31")` answers `2026-07-01` with no `NaN` anywhere, so a hand-edited export moved that day's OT into the **next month's payroll period** — hence a round-trip comparison (`toISOString().slice(0,10) === text`) and an arm that goes red if anyone "simplifies" it back to `isNaN`. 🔴 A duplicate `(staffId, date)` pair was last-wins in silence under the upsert loop and would **throw** on `@@unique` under `createMany`, so last-wins is now pinned explicitly, position preserved. Plus the narrowed skip stated as a whole — the old blank-lines test passed on `rows).toHaveLength(1)` while three lines vanished for three different reasons — and the `(บรรทัดไม่มีชื่อผู้ใช้)` bullet, since `byUsername.get("")` would otherwise push an empty one. Counter-tested 2026-09-22, one mutant at a time per ใบ 067: round-trip → plain `isNaN` **1 fail** · the skip back to `!user \|\| !date \|\| !hours` **3 fail** · dedup → plain `rows.push` **1 fail**. 🔴 **Round 2 of review narrowed the skip a second time — `!user && !date` → `!line.trim()` — and the pin stayed at 12 on purpose**: the case is a new *arm* inside the existing skip test, not a new test, so a raise here would have been a number with nothing behind it. `payroll-auditor` found the last line shape still falling into no bucket: **hours with neither identity field** (`,,176`, `<TAB><TAB>11.5`), which is what a CSV writes when the name column is blank for a scan the reader could not identify — `max(0, 11.5 − 9) × 40 = 100 ฿` gone with nothing on screen, §2 rule 4's exact shape. The predicate got *simpler* rather than wider, and both arms key on `""`, so they collapse into the `(บรรทัดไม่มีชื่อผู้ใช้)` bullet the screen already renders — no fourth box. Pinned by **whole-array** compare, because a length assertion stays green if either arm stops arriving; mutant `!line.trim()` → `!user && !date` **1 fail**. ⚠️ The deliberate cost is that a separators-only line (`,,`) is no longer blank either and now shows that one bullet on an otherwise clean paste — a visible non-event, bought with the skip that was losing the 100 ฿. ⚠️ What it does **not** prove is the rollback: `bun test` has no database, and the lane that would is `tasks/todo/015-db-test-lane.md` |
| `lib/ot-import.test.ts` | 12 → 13 | ใบ 072 — the raise buys `calendarDate` pinned **as a helper**, which is a different thing from what the row above bought. Until this card it was module-private: everything asserted about it went through `parseOtPaste`, so its contract was a by-product of one caller's tests. ใบ 072 exports it because `/ot`'s **one-row `add` form** had the identical hole the paste path closed at 014 — `new Date(String(formData.get("date")) + "T00:00:00Z")` straight into `db.otEntry.upsert`, so `POST date=2026-06-31` stored `2026-07-01`: `max(0, 12 − 9) × 40 = 120 ฿` left June for July, **and** the `(staffId, date)` upsert key overwrote that person's real 1 July row, destroying a true record in silence (§2 rule 4). One predicate, two callers ⇒ a shared predicate that only one caller ever asserts is one tidy-up away from changing silently for the other, which is why the new test is its own `describe` and calls it directly. It takes both columns — **unparseable** (`2026-13-01`, `2026-7-1`, `2026-07-01x`, `""`) and **silently rolled over** (`2026-06-31`, `2026-02-31`, `2026-02-29`) — plus real days including a **genuine** leap day (`2024-02-29`), plus the padded `" 2026-07-01"` that documents why both callers must `.trim()` before calling (the round-trip compares against the text it is handed, so an untrimmed form field would make the two paths refuse different sets — the exact divergence this card removes). 🔑 The load-bearing assertion is `Number.isNaN(new Date(rolled).getTime()) === false` sitting **next to** the refusal: it states in the file that an `isNaN` check is green over precisely these inputs, so the round-trip cannot be "simplified" while the test still passes. Counter-tested 2026-09-22, one mutant at a time per ใบ 067: round-trip → plain `isNaN` now **2 fail** (the ใบ 014 paste arm and this one) · a naive end-of-month cap in front of the round-trip (`day >= 29 ⇒ null` — the plausible wrong fix for the rollover column) **1 fail, this test alone**, because every paste arm's good rows are early-month days ⇒ the genuine leap day is what makes that arm load-bearing rather than decorative. ⚠️ What it does **not** prove is the guard in `app/ot/page.tsx` itself: a server action needs a database (`tasks/todo/015-db-test-lane.md`), so `add` calling this helper, in this order, is reviewed and not tested |
| — (**no pin, and no test**) | ใบ 080 — three more money writes wired onto `calendarDate` | The first entry here for a card that raised **nothing**, written because the honest answer needs a record as much as a raise does. ใบ 080 put that helper in front of `db.sale.create`, `db.classSession.create` and `/sync/review`'s `db.teachSession.update`, and made two silent `return`s loud. **Every one of those is wiring inside a server action** ⇒ what would be asserted is the *order* of guards and which `?err=` a `redirect` carries, both of which need a database (`tasks/todo/015-db-test-lane.md`) — the same wall the ใบ 072 row above ends on for `app/ot/page.tsx`. The predicate itself is already pinned at 13 and none of its behaviour moved, so a raise here could only have been paid for by a test invented to justify it: an arm that re-asserts `calendarDate` under a new name proves the pin went up and nothing else. ⇒ **reviewed, not tested**: the date guard first on all three, `.trim()` on all three, `date` reaching the `create`/`update` in place of `new Date(text + "T00:00:00Z")`, and the four flags (`err=date` ×3 · `err=need` · `err=stale` · the added `redirect(back)`) each having a notice that renders. ⚠️ Ranked by what it costs to be wrong, this is the **most** under-covered money path in the repo now: `/sync/review` is where a human retypes a date by hand and the write is `reviewed: true`, so nothing repairs it later |
| `lib/date-window.test.ts` | **new at 6, 6 → 8 → 10 → 11 → 12 in the same card** | ใบ 082 — the plausible operating window, as a pure predicate the five money forms share. `calendarDate` answers *does this day exist* and it is **right** about `0226-06-05`: the 5th of June of the year 226 is a real day and round-trips exactly as typed, so it walked through every ใบ 014/072/080 guard and landed outside **every** `periodRange` ⇒ in no payslip at all. On `/sync/review` that is permanent (`status: "ok", reviewed: true` ⇒ the คาบ leaves the queue and no sync repairs it: **250 ฿ paid as 0 ฿**, queue clean). 🔴 **The bounds are the thing a future reader will want to shrink, so the pin is mostly bounds**: the card's whole measured table (`0226-06-05` `0206-06-05` `0026-06-05` `9999-12-31` `0001-01-01`) refused, plus **both edges accepted** — a date exactly at `earliest`, a date exactly at `now + date.futureDays` — and the two days one step outside them refused. That pairing is what makes a tightening red instead of silent, and it is the §2 rule 4 **mirror** direction: a window too tight refuses a legitimate back-dated correction, which is hours or a bill not paid. Two more arms buy traps that are invisible when they fire: a **two-digit** `date.earliestYear` must be that year, because `Date.UTC(year, …)` maps `0..99` onto `1900 + year` ⇒ `26` would become **1926** and the window would swallow this whole class of defect; and a blank or missing key must **throw** through `num()` rather than default to 0 (`earliest` at year 0 and `latest` at today = the guard running backwards). Counter-tested 2026-09-24, one mutant at a time per ใบ 067: `new Date(0)` + `setUTCFullYear` → `Date.UTC(year, 0, 1)` **1 fail** (the two-digit arm, and only it) · inclusive `>=`/`<=` → exclusive `>`/`<` **1 fail** (the edges arm, and only it — every other arm's dates are interior). ⚠️ What it does **not** prove is any of the five call sites: four are server actions and one is a parse called from one, so the *order* of the guards and which `?err=` each `redirect` carries are reviewed, not tested — the same wall as ใบ 072/080 above (`tasks/todo/015-db-test-lane.md`) | · **6 → 8, ใบ 082's own fix round** (`payroll-auditor`): the silent-zero arm above closes a window that is *wrong*; these two close a window that is **empty**. Nothing checked `earliest <= latest`, and `/admin/config`'s `cfg` arm accepts any non-negative finite number ⇒ `date.earliestYear = 2027` saves cleanly and every money write in the app is refused at once (~640 ฿/day of OT alone, inside §6's three-day window for variable pay), with **nothing on screen saying why** — the guard running backwards, which is the exact failure the module header already argues about for `0`. The second arm is the unrepresentable bound: `date.futureDays` above ~1e8 leaves `latest` an **Invalid Date**, so `withinWindow` is false for everything *and* the four pages' `renderWindow.toISOString()` raise `RangeError` outside any try/catch ⇒ a server error page with the Thai cause redacted in production. 🔑 Both arms assert the **message**, not merely that something threw — `/กลับด้าน/`, and the offending key's own name — because a throw that names the wrong key sends the reader to `/admin/config` to fix a key that is already right; and the inverted arm pins the neighbour that must **not** throw (`earliest === latest` is a one-day window, tight but honest, and a `>=` guard would refuse it) · **8 → 10, ใบ 082's *second* fix round** (`payroll-auditor` again, this time on a regression the first round introduced). One arm is the **ordering** of the three `Number.isNaN` checks: `now` is tested before the two config bounds because an invalid `now` makes `latest` NaN as well, so the obvious tidy-up — grouping the three checks by shape — reorders it, stays green at 8/8, and reports a caller's clock fault as `config date.futureDays อยู่นอกช่วง: 31`, sending whoever reads it to fix a key that is already right. The arm asserts the **message** (`/now ไม่ใช่วันที่/`); a bare `.toThrow()` survives exactly that reordering. The other arm is `windowForRender`, which is the fix itself: the throw is correct on the **action** path and was a regression on the **render** path, where it took each page's *correction* surface down with its write surface — before it existed, an inverted window left all four screens rendering and refused only the dated writes, so a duplicate row could still be seen and deleted. Measured cost of the regression: a 30,000 ฿ Premium membership entered twice on `/sales` can be neither seen nor removed while the key is wrong, and a run approved first pays `30,000 × 10%` **twice** — 6,000 ฿ instead of 3,000 ฿ (§2.2 `comm.membership.full`); 250 ฿ a duplicate ว่ายน้ำ คาบ on `/classes`. The arm pins both outcomes — the fault **string** that reaches the amber box (`/กลับด้าน/` plus both offending numbers, because a fault reading only "ตั้งค่าผิด" is the refusal §2 rule 4 calls useless) and the two formatted bounds the four pages print — so putting `dateWindow` back in a page body goes red here. ⚠️ Not pinned and said so in the file: the `throw e` arm for a non-`DateWindowError`. Every failure the module can decide on is tagged (including `num()`'s, via `bound`), so the arm has no reachable input; the narrowness of the catch is a type-level guarantee, reviewed. Counter-tested 2026-09-24, one mutant at a time: try/catch removed **1 fail** (the `windowForRender` arm, and only it) · `now` check moved below the two bound checks **1 fail** (the ordering arm, and only it) · `instanceof` dropped **green**, which is why it is documented rather than claimed · **10 → 11, third fix round** — which **supersedes the ⚠️ just above**: the green `instanceof` mutant was hiding a real hole rather than proving a type-level guarantee. `num()` now throws its own `ConfigError` and `bound()` re-tags **only** that, so a `cfg` that is not the shape it claims raises an ordinary `TypeError` that reaches `app/error.tsx` and the log instead of an amber box on four screens blaming the two date keys. The arm is pinned as **not** a `DateWindowError`, and the same mutant now comes back **1 fail**. · **11 → 12, fourth fix round** (`payroll-auditor`) — the other half of that same delta, and the half that carries the money. Round 3 pinned that `dateWindow` **throws** on a blank or missing bound; nothing pinned what `windowForRender` does with one, which is the precise case `ConfigError` was created for. Counter-tested: revert those three throws from `ConfigError` back to plain `Error` — one word, the edit a refactor makes without thinking, `bound()`'s narrow catch left exactly as shipped — and the suite stayed **green at 243 pass / 3 skip / 0 fail** while `windowForRender` went back to throwing ⇒ `/ot`, `/sales`, `/classes` and `/sync/review` 500 on a key that is merely absent, instead of printing the box `app/_components/window-fault-notice.tsx` describes in prose and whose heading was rewritten to be true of exactly this case. The arm asserts the **message** of both shapes (`/ไม่พบ config/`, `/เว้นว่าง/`): `num()`'s two sentences are two different remedies, since a missing key has no field on `/admin/config` at all (ใบ 044) while a blanked one has. ⚠️ **Reachability, stated correctly here because a review round stated it wrongly:** this is **not** reachable on the deployed site. `docker-compose.yml` makes `app` wait on the `migrate` service (`condition: service_completed_successfully`), `migrate`'s command ends in `bun run prisma/seed.ts`, and the seed backfills every missing `CONFIG_DEFAULTS` key on every `docker compose up -d --build`. The reachable states are an **unseeded developer database or a row deleted by hand** — and the pin is required anyway, because what it holds is a documented behaviour against a one-word refactor, which does not depend on how the bad state is reached. No number moved for the round's second edit: the non-`DateWindowError` arm's sentinel now asserts `toBeInstanceOf(TypeError)` with `expect.hasAssertions()`, because as a bare `not.toBeInstanceOf` the sentinel `Error` thrown when `windowForRender` *stops* throwing landed in that same `catch` and satisfied it — a guard that was green in exactly the case it was written to catch
| `lib/ot-import.test.ts` | 13 → 14 | ใบ 082 — `parseOtPaste` takes the window as a **required** argument (required, because the parse has no page context and an optional one is the one a caller forgets, which would leave the path OT actually arrives by open while the one-row form beside it stayed shut) and routes a real day in an impossible year into its **own** `outOfWindowDates` bucket. 🔑 The load-bearing assertion is `expect(invalidDates).toHaveLength(1)` — **one**, the genuinely unreadable line, and not the two well-formed ones beside it. Folding them into `invalidDates` was the cheap fix and it reports the **wrong cause**: the date reads perfectly, so "วันที่อ่านไม่ออก" sends the operator hunting a typo that is not there. §2 rule 4 is about the warning being *useful*, not merely present, and this is the first pin in the table that asserts a bucket's **boundary** rather than its membership. One fixture carries both impossible years, the unreadable line and the good row (§7: the expensive unit is the fixture). Counter-tested 2026-09-24: routing the out-of-window line into `invalidDates` instead of its own bucket — the cheap fix this arm exists to forbid — **1 fail**. ⚠️ The bounds themselves are **not** re-asserted here — the file uses a written-out window fixture on purpose, so a moved config default cannot make the parse's cases fail for a reason that is not theirs |

🔑 **What makes a split honest rather than a quiet write-down is the sum**: task 037's
6 + 6 + 14 + 3 + 3 + 4 = **36**, against the 34 that moved plus the 2 the card added. A split whose
rows sum to *less* than the row they replace is a coverage removal wearing a refactor's clothes, and
§7's rule applies to it unchanged — the reason goes in the task card, never "to make the gate green".
The split rule itself, including the half of the red that a split does **not** produce, is in
[gate-tiers-and-pins.md](gate-tiers-and-pins.md).
