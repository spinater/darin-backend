node_modules
.next
generated
.env
*.tsbuildinfo
next-env.d.ts

# ข้อมูลลูกค้าจริง (ชื่อ+เบอร์โทร) — ห้ามขึ้น git
docs/
client_secret_*.json
*.credentials.json

# ที่ทำงานชั่วคราวของ agent/เกต (CLAUDE.md §6 rule 5: ห้ามใช้ /tmp)
.scratch/

# graphify context graph — machine-written and rebuildable (CLAUDE.md §8)
graphify-out/
